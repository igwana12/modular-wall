---
phase: 11-security-routing-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/claw2501/.claude/hooks/gsd-prompt-guard.js
  - /Users/claw2501/.claude/settings.json
autonomous: true
requirements: [APPR-01, APPR-02, APPR-03]

must_haves:
  truths:
    - "Bash commands containing Slack channel names other than #pantheon or #the-orb trigger advisory warning"
    - "Bash commands containing git push to main/master trigger advisory warning"
    - "Write/Edit operations targeting .env, credentials, or secret files trigger advisory warning"
    - "Legitimate operations (health check posting to #the-orb) pass without warning"
  artifacts:
    - path: "/Users/claw2501/.claude/hooks/gsd-prompt-guard.js"
      provides: "Extended prompt guard with Slack, git push, and secret file rules"
      contains: "pantheon"
    - path: "/Users/claw2501/.claude/settings.json"
      provides: "Hook configuration with Bash matcher for prompt guard"
      contains: "Bash"
---

<objective>
Extend gsd-prompt-guard.js with three new safety rules: (1) Slack channel restriction to #pantheon and #the-orb only, (2) main branch push confirmation, and (3) secret file write protection. Update settings.json to also trigger the guard on Bash commands.

Purpose: Tighten approval boundaries so Claude Code sessions cannot accidentally post to wrong Slack channels, push directly to main, or overwrite credential files.
Output: Updated gsd-prompt-guard.js with new rules, updated settings.json hook matcher.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task id="1" type="auto">
<name>Extend gsd-prompt-guard.js with Slack, git push, and secret file rules</name>
<action>
Refactor gsd-prompt-guard.js to handle three hook event types:
1. PreToolUse for Write|Edit — existing prompt injection scanning PLUS new secret file guards
2. PreToolUse for Bash — new Slack channel and git push guards

Add these rule sets:
- SLACK GUARD: When Bash command references a Slack channel (via #channel-name, channel ID, or slack_post), warn if the channel is NOT #pantheon (C0APJ8ZL752) or #the-orb. Allow any command that targets these two channels.
- GIT PUSH GUARD: When Bash command contains "git push" targeting main or master branch, emit advisory warning requesting explicit confirmation.
- SECRET FILE GUARD: When Write or Edit targets files matching .env, credentials, secrets, tokens, or key files, emit advisory warning.

All guards are ADVISORY (do not block) — consistent with the existing prompt injection guard behavior.
</action>
<done_criteria>
- gsd-prompt-guard.js contains Slack channel allowlist with #pantheon and #the-orb
- gsd-prompt-guard.js detects git push to main/master
- gsd-prompt-guard.js detects writes to secret/credential files
- All guards emit advisory warnings (not blocks)
- Existing prompt injection scanning still works unchanged
</done_criteria>
<verify>
node -c /Users/claw2501/.claude/hooks/gsd-prompt-guard.js
echo '{"tool_name":"Bash","tool_input":{"command":"slack-post.sh req123 hello #general"}}' | node /Users/claw2501/.claude/hooks/gsd-prompt-guard.js
echo '{"tool_name":"Bash","tool_input":{"command":"git push origin main"}}' | node /Users/claw2501/.claude/hooks/gsd-prompt-guard.js
echo '{"tool_name":"Write","tool_input":{"file_path":"/tmp/.env","content":"SECRET=abc"}}' | node /Users/claw2501/.claude/hooks/gsd-prompt-guard.js
echo '{"tool_name":"Bash","tool_input":{"command":"slack-post.sh req123 hello #the-orb"}}' | node /Users/claw2501/.claude/hooks/gsd-prompt-guard.js
</verify>
</task>

<task id="2" type="auto">
<name>Update settings.json hook matcher to include Bash</name>
<action>
Update the PreToolUse matcher in ~/.claude/settings.json from "Write|Edit" to "Write|Edit|Bash" so the prompt guard also fires on Bash commands (needed for Slack and git push guards).
</action>
<done_criteria>
- settings.json PreToolUse matcher includes "Bash"
- Other hook configurations unchanged
</done_criteria>
<verify>
cat /Users/claw2501/.claude/settings.json | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); const m=d.hooks.PreToolUse[0].matcher; console.log('Matcher:', m); console.log(m.includes('Bash') ? 'PASS' : 'FAIL')"
</verify>
</task>

<task id="3" type="auto">
<name>Verify all guards with integration tests</name>
<action>
Run a comprehensive test suite against the updated prompt guard:
1. Slack to #general (should warn)
2. Slack to #the-orb (should pass)
3. Slack to #pantheon (should pass)
4. git push origin main (should warn)
5. git push origin feature-branch (should pass)
6. Write to .env file (should warn)
7. Write to normal .ts file (should pass)
8. Existing prompt injection detection still works
</action>
<done_criteria>
- All 8 test cases produce expected results
- No false positives on legitimate operations
- No false negatives on restricted operations
</done_criteria>
</task>

</tasks>

<verification>
All three APPR requirements met:
- APPR-01: Slack messages to channels other than #pantheon/#the-orb trigger warning
- APPR-02: git push to main triggers warning
- APPR-03: Writes to .env/credentials files trigger warning
</verification>

<success_criteria>
The prompt guard catches all three unsafe operation types while allowing legitimate operations through without warnings.
</success_criteria>
