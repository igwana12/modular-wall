---
phase: 12-session-mobility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-session-mobility/session-mobility-test-results.md
  - .planning/phases/12-session-mobility/session-mobility-cheat-sheet.md
autonomous: false
requirements: [MOBIL-01, MOBIL-02, MOBIL-03]

must_haves:
  truths:
    - "User can start a Claude Code session on Mac and access it from phone browser via /remote-control"
    - "User can teleport a web session back into a CLI terminal"
    - "Conversation history and file context survive the round trip between devices"
    - "A cheat sheet exists documenting all session mobility commands"
    - "Test results document what works, what doesn't, and known limitations"
  artifacts:
    - path: ".planning/phases/12-session-mobility/session-mobility-test-results.md"
      provides: "Round-trip test results with pass/fail per step"
      contains: "## Test Results"
    - path: ".planning/phases/12-session-mobility/session-mobility-cheat-sheet.md"
      provides: "Quick reference for teleport, remote-control, resume, name"
      contains: "## Session Commands"
  key_links:
    - from: "/remote-control"
      to: "claude.ai/code"
      via: "Browser bridge"
      pattern: "remote.*control"
    - from: "/teleport"
      to: "CLI session"
      via: "Web-to-terminal resume"
      pattern: "teleport"
---

<objective>
Test Claude Code's session mobility features (teleport, remote-control, resume, name) end-to-end and document the results as a cheat sheet.

Purpose: Verify that cross-device session mobility works reliably so the user can start work on Mac and continue on phone/iPad without losing context. Per D-13, no new code -- configuration and documentation only.
Output: Test results document + session mobility cheat sheet
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-session-mobility/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify CLI features and create test script</name>
  <files>.planning/phases/12-session-mobility/session-mobility-test-results.md</files>
  <read_first>
    - Run `claude --help` to confirm --resume, --continue, --name flags exist
    - Run `claude --version` to capture current version
    - Read `~/.claude/cache/changelog.md` lines mentioning teleport or remote-control (if accessible)
  </read_first>
  <action>
    1. Capture `claude --version` output and `claude --help` output. Record which session mobility flags are available (--resume, --continue, --name, --chrome). Per D-01 through D-04, confirm feature availability.

    2. Create the test results document at `.planning/phases/12-session-mobility/session-mobility-test-results.md` with this structure:
       - Header with date, Claude Code version, OS
       - Test matrix table with columns: Test, Command, Expected, Actual, Pass/Fail
       - Tests to include (per D-05, D-06):
         a. `--name` flag: Start session with `claude --name orb-hardware`, verify name appears
         b. `--resume` flag: Exit session, resume with `claude --resume`, verify history intact
         c. `/remote-control`: Start session, run /remote-control, capture the URL/code generated
         d. Phone access: Open URL on phone browser (CHECKPOINT -- human does this)
         e. Context check: After remote access, verify conversation history visible on phone
         f. `/teleport`: From web session, teleport back to CLI terminal
         g. Round-trip context: Verify file context and conversation history survived full round trip
       - Limitations section (per D-07): document anything that doesn't work (MCP servers in web mode, tool calls, etc.)

    3. Execute tests a-c on the desktop:
       - Run `claude --name test-mobility` and verify the session starts with that name
       - Exit and run `claude --resume` to verify session resumption
       - Start a new named session and run `/remote-control` -- capture the output (URL or pairing code)
       - Record all results in the test matrix (pass/fail with actual behavior)

    4. Fill in desktop-side test results. Leave phone-side tests (d, e, f, g) marked as "PENDING -- requires human verification" for Task 2.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-session-mobility/session-mobility-test-results.md && grep -c "Pass\|Fail\|PENDING" .planning/phases/12-session-mobility/session-mobility-test-results.md</automated>
  </verify>
  <acceptance_criteria>Test results file exists with desktop-side tests completed (pass/fail recorded for --name, --resume, /remote-control) and phone-side tests marked PENDING</acceptance_criteria>
  <done>Desktop-side CLI tests executed with results recorded. Phone-side tests ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Round-trip phone verification</name>
  <files>.planning/phases/12-session-mobility/session-mobility-test-results.md</files>
  <what-built>Desktop-side session mobility tests are complete. The /remote-control command has been run and a URL or pairing code is available. Now the user needs to physically test the phone/browser side of the round trip (per D-05, D-06).</what-built>
  <action>
    Present the user with the remote-control URL or pairing code from Task 1 results. Wait for the user to complete the phone-side verification steps and report back with PASS/FAIL results for each step.
  </action>
  <how-to-verify>
    1. Open the remote-control URL (from Task 1 results) on your iPhone/iPad browser
    2. Verify you can see the conversation history from the Mac session (MOBIL-03)
    3. Issue a command from the phone browser (e.g., ask Claude a question or run a simple file read)
    4. Back on Mac terminal, verify the command executed and output appeared
    5. If /teleport is available in the web interface, try teleporting the session back to a CLI terminal
    6. After round trip, verify conversation context is intact (MOBIL-01, MOBIL-03)
    7. Note any limitations: MCP servers unavailable? Tool calls restricted? Latency issues? (D-07)
    8. Report results: for each step above, state PASS or FAIL with a brief note
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: requires human verification of phone-side round trip"</automated>
  </verify>
  <done>User has reported PASS/FAIL results for all phone-side verification steps, or reported that features are unavailable.</done>
  <resume-signal>Provide results for steps 1-7 (PASS/FAIL per step + any limitations discovered). Type "skip" if remote-control is not available in current version.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Document results and create cheat sheet</name>
  <files>
    .planning/phases/12-session-mobility/session-mobility-test-results.md
    .planning/phases/12-session-mobility/session-mobility-cheat-sheet.md
  </files>
  <read_first>
    - .planning/phases/12-session-mobility/session-mobility-test-results.md (current state from Task 1)
    - User's feedback from Task 2 checkpoint
  </read_first>
  <action>
    1. Update the test results document with the user's phone-side verification results from Task 2. Fill in the PENDING rows with actual pass/fail outcomes and observations.

    2. Add a "Limitations" section documenting any issues discovered (per D-07):
       - Which features worked as expected
       - Which features had issues and what the workarounds are
       - MCP server availability in web mode
       - Tool call restrictions in remote sessions
       - Any latency or UX issues

    3. Create the session mobility cheat sheet at `.planning/phases/12-session-mobility/session-mobility-cheat-sheet.md` (per D-11) with:

       **Session Naming Convention** (per D-08, D-09, D-10):
       - Format: `project-task` (e.g., `jarvis-frontend`, `orb-hardware`, `sc-content`)
       - Maps to Phase 11 context profiles: orb, jarvis, hardware, creative
       - Apply via: `claude --name jarvis-frontend` or `/name jarvis-frontend` mid-session

       **Quick Reference Table**:
       | Command | What It Does | When To Use |
       | `claude --name X` | Name a session | Session start |
       | `claude --resume` | Resume last session | Continue previous work |
       | `claude --continue` | Continue conversation | Pick up where you left off |
       | `/remote-control` | Bridge to browser | Access from phone/tablet |
       | `/teleport` | Pull web session to CLI | Return to terminal |
       | `/rc` | Remote control mode | TUI compat (use --text) |

       **Round-Trip Workflow** (per D-05):
       Step-by-step: start named session -> /remote-control -> open on phone -> work -> /teleport back

       **Known Limitations** (from test results):
       Document whatever was discovered in testing

    4. Per D-12, ensure test results are clearly documented with pass/fail status for each mobility feature tested.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-session-mobility/session-mobility-cheat-sheet.md && grep -c "remote-control\|teleport\|resume\|name" .planning/phases/12-session-mobility/session-mobility-cheat-sheet.md</automated>
  </verify>
  <acceptance_criteria>
    - Test results document updated with all phone-side results (no PENDING rows remaining)
    - Cheat sheet exists with session naming convention (D-08), command reference, round-trip workflow, and limitations
    - No new code created (per D-13)
  </acceptance_criteria>
  <done>Complete test results document and session mobility cheat sheet published. All MOBIL requirements verified or limitations documented.</done>
</task>

</tasks>

<verification>
1. Test results document exists with all tests showing pass/fail (no PENDING)
2. Cheat sheet exists with command reference and session naming convention
3. No new code files were created (per D-13 -- documentation only)
4. Limitations section documents any features that don't work as expected
</verification>

<success_criteria>
- Round-trip test (Mac -> phone -> Mac) attempted and results documented
- Session naming convention established matching Phase 11 context profiles
- Cheat sheet is a usable quick reference for daily session mobility
- All 3 MOBIL requirements addressed (verified working or limitations documented)
</success_criteria>

<output>
After completion, create `.planning/phases/12-session-mobility/12-01-SUMMARY.md`
</output>
