---
phase: 07.3-media-orchestration-engine
plan: 03
subsystem: media-orchestration
tags: [storyboard, story-stage-manager, beat-advance, show-presentation, obsidian-myths]
dependency_graph:
  requires:
    - phase: 07.3-01
      provides: [script_planner.py, parallel_fetcher.py, _orchestrate_with_storyboard]
  provides:
    - get_stage_manager() singleton for myth note indexing
    - match_story() and get_plan() APIs on StoryStageManager
    - StagePlan context injection into script_planner prompt
    - Beat-based banner advance at segment boundaries
    - show_presentation activation via StagePlan multi-channel context
  affects: [server.py, script_planner.py, story_stage_manager.py]
tech_stack:
  added: []
  patterns: [lazy-singleton, stage-plan-injection, beat-based-banner-advance]
key_files:
  created: []
  modified:
    - ~/.openclaw/workspace/jarvis-voice-v2/backend/media_director/story_stage_manager.py
    - ~/.openclaw/workspace/jarvis-voice-v2/backend/media_director/script_planner.py
    - ~/.openclaw/workspace/jarvis-voice-v2/backend/server.py
key-decisions:
  - "match_story returns single best plan (score>=5.0) via existing hybrid keyword+vector match"
  - "StagePlan injected as Claude system prompt context, not structured tool parameters"
  - "Banner advance events embedded in storyboard segment data array, not separate event stream"
  - "Banner waypoints sourced from StagePlan.map_locations first, then show_presentation fallback"
patterns-established:
  - "Lazy singleton: get_stage_manager() with safe init (missing vault = 0 plans, no crash)"
  - "Beat-based advance: segment boundaries = narrative beats = banner transitions"
requirements-completed: [ORCH-05]
metrics:
  duration: 2min
  completed: 2026-04-01
---

# Phase 07.3 Plan 03: Story Stage Manager Integration + Beat-Based Banner Advance Summary

**Wired 186 Obsidian myth stage plans into script planner as Claude context, beat-based banner advance at narrative transitions, show_presentation activation via multi-channel StagePlan hints**

## Performance

- **Duration:** 2 min
- **Started:** 2026-04-01T00:36:17Z
- **Completed:** 2026-04-01T00:38:26Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- StoryStageManager now has match_story() and get_plan() APIs plus get_stage_manager() lazy singleton
- plan_script() accepts StagePlan and injects locations, images, characters, effects, mood into Claude prompt with show_presentation guidance
- _orchestrate_with_storyboard() looks up StagePlan via match_story() before script planning
- Banner waypoints advance at segment boundaries (narrative beats) instead of evenly spaced clock ticks
- Existing _build_banner_advance_events() unchanged for non-storyboard flows (briefings, concierge)
- Server starts cleanly even if Obsidian vault path doesn't exist

## Task Commits

Each task was committed atomically:

1. **Task 1: Wire story_stage_manager into script planner** - `4ad837d` (feat)
2. **Task 2: Beat-based banner advance + server.py wiring** - `2de5167` (feat)

## Files Created/Modified
- `backend/media_director/story_stage_manager.py` - Added match_story(), get_plan() aliases, get_stage_manager() singleton
- `backend/media_director/script_planner.py` - Added stage_plan parameter, StagePlan context injection with show_presentation guidance
- `backend/server.py` - Import get_stage_manager, lookup StagePlan before plan_script, beat-based briefing_advance events at segment boundaries

## Decisions Made
- **match_story as alias**: Uses existing hybrid match() (keyword + vector) with score threshold 5.0 -- no new scoring logic needed
- **StagePlan as prompt injection**: Context goes into Claude system prompt as plain text, not structured tool parameters -- Claude sees the full picture and decides which tools to call
- **Banner advance in segment data**: briefing_advance events embedded in each segment's data array (dispatched by StoryboardScheduler at segment offset_s) rather than a separate event stream
- **Map locations as waypoints**: StagePlan.map_locations used as banner waypoints first; falls back to extracting from show_presentation ws_messages

## Deviations from Plan

None -- plan executed exactly as written.

## Known Stubs

None -- all functions are fully implemented with real logic.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Story stage manager fully wired into orchestration pipeline
- show_presentation activation depends on Claude seeing rich StagePlan context (locations + images + characters) and choosing to use it
- Plan 02 (frontend StoryboardScheduler) handles dispatching the storyboard segments including beat-based banner advance events

---
*Phase: 07.3-media-orchestration-engine*
*Completed: 2026-04-01*
