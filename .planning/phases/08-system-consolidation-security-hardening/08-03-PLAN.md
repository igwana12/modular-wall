---
phase: 08-system-consolidation-security-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - /Volumes/Extreme Pro/ACTIVE/smithers/.gitignore
  - /Volumes/Extreme Pro/ACTIVE/llm-router/.gitignore
  - /Volumes/Extreme Pro/ACTIVE/sacred-circuits-v2/.gitignore
  - /Users/claw2501/services/smithers/.claude/worktrees/
autonomous: true
requirements: [SEC-04, SEC-05]

must_haves:
  truths:
    - "Smithers on Extreme Pro has a git repo with initial commit"
    - "LLM Router on Extreme Pro has a git repo with initial commit"
    - "Sacred Circuits Pipeline v2 on Extreme Pro has a git repo with initial commit"
    - "All stale agent worktrees removed from services/smithers/"
  artifacts:
    - path: "/Volumes/Extreme Pro/ACTIVE/smithers/.git"
      provides: "Smithers version control"
    - path: "/Volumes/Extreme Pro/ACTIVE/llm-router/.git"
      provides: "LLM Router version control"
    - path: "/Volumes/Extreme Pro/ACTIVE/smithers/.gitignore"
      provides: "Smithers git exclusions"
      contains: ".env"
  key_links:
    - from: "/Volumes/Extreme Pro/ACTIVE/smithers/.gitignore"
      to: ".env files"
      via: "gitignore exclusion"
      pattern: "\\.env"
---

<objective>
Version-control the three critical services on Extreme Pro (Smithers, LLM Router, Sacred Circuits v2) with initial commits and proper .gitignore. Clean all 9 stale agent worktrees from services/smithers/.

Purpose: 1TB+ of critical service code is completely unversioned on the external drive. One accidental delete or drive failure loses everything. Stale worktrees contain outdated server.py copies that could cause confusion.
Output: Three git repos with initial commits, cleaned worktree directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-system-consolidation-security-hardening/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Git init critical services on Extreme Pro</name>
  <files>/Volumes/Extreme Pro/ACTIVE/smithers/.gitignore, /Volumes/Extreme Pro/ACTIVE/llm-router/.gitignore, /Volumes/Extreme Pro/ACTIVE/sacred-circuits-v2/.gitignore</files>
  <read_first>
    /Volumes/Extreme Pro/ACTIVE/smithers/server.py
    /Volumes/Extreme Pro/ACTIVE/llm-router/server.py
  </read_first>
  <action>
1. First, discover the actual directory names for the three services:
   ls "/Volumes/Extreme Pro/ACTIVE/" | grep -i -E "smith|llm|sacred|router|pipeline"
   Use the actual names found (the CONTEXT.md lists them but names may differ on disk).

2. For EACH of the three services (Smithers, LLM Router, Sacred Circuits Pipeline):

   a. Check if .git already exists (skip if so):
      test -d "/Volumes/Extreme Pro/ACTIVE/{service}/.git"

   b. Create .gitignore with standard Python exclusions:
      ```
      # Environment and secrets
      .env
      .env.*
      *.kdbx

      # Python
      __pycache__/
      *.pyc
      *.pyo
      .venv/
      venv/
      env/

      # Node
      node_modules/

      # IDE
      .idea/
      .vscode/
      *.swp

      # Data (large files)
      *.db
      *.sqlite
      *.log
      data/*.json

      # OS
      .DS_Store
      Thumbs.db

      # Claude worktrees
      .claude/worktrees/
      ```

   c. Initialize git and create initial commit:
      cd "/Volumes/Extreme Pro/ACTIVE/{service}"
      git init
      git add .gitignore
      git add -A
      git status (review what's being committed — ensure NO .env files)
      git commit -m "chore: initial commit — version control for {service}"

   d. Verify: git log --oneline should show exactly 1 commit.

3. If Sacred Circuits Pipeline v2 directory name is different (could be "sacred-circuits" or "sc-pipeline-v2" etc.), find it:
   find "/Volumes/Extreme Pro/ACTIVE" -name "*.py" -path "*sacred*" -o -name "*.py" -path "*pipeline*" | head -5
  </action>
  <verify>
    <automated>bash -c 'for dir in "/Volumes/Extreme Pro/ACTIVE/smithers" "/Volumes/Extreme Pro/ACTIVE/llm-router"; do if [ -d "$dir/.git" ]; then echo "GIT OK: $dir"; else echo "GIT MISSING: $dir"; fi; done'</automated>
  </verify>
  <acceptance_criteria>
    - Each service directory has a .git/ folder
    - Each has a .gitignore that excludes .env, __pycache__, .venv, node_modules, .claude/worktrees/
    - Each has at least 1 commit (git log --oneline returns at least 1 line)
    - NO .env files were committed (git show --stat HEAD shows no .env)
  </acceptance_criteria>
  <done>Three critical services on Extreme Pro are version-controlled with proper .gitignore excluding secrets</done>
</task>

<task type="auto">
  <name>Task 2: Remove stale agent worktrees from services/smithers</name>
  <files>/Users/claw2501/services/smithers/.claude/worktrees/</files>
  <read_first>
    /Users/claw2501/services/smithers/.claude/worktrees/agent-a015414f/server.py
  </read_first>
  <action>
1. List all worktrees and their sizes:
   du -sh /Users/claw2501/services/smithers/.claude/worktrees/agent-*

2. Verify these are indeed stale copies by checking the diff against current server.py:
   diff /Users/claw2501/services/smithers/server.py /Users/claw2501/services/smithers/.claude/worktrees/agent-a015414f/server.py | head -20
   (If they differ, the worktree is outdated. If identical, still stale since no active agent session.)

3. Record the count and total size before removal.

4. Remove ALL agent worktrees:
   rm -rf /Users/claw2501/services/smithers/.claude/worktrees/agent-*

5. Verify the directory is clean:
   ls /Users/claw2501/services/smithers/.claude/worktrees/
   (Should be empty or contain only non-agent directories)

6. Check if the worktrees/ directory itself can be removed:
   - If empty, rmdir it
   - If it contains other needed files, leave it
  </action>
  <verify>
    <automated>bash -c 'COUNT=$(ls /Users/claw2501/services/smithers/.claude/worktrees/agent-* 2>/dev/null | wc -l); echo "Remaining agent worktrees: $COUNT"; [ "$COUNT" -eq 0 ] && echo "PASS" || echo "FAIL"'</automated>
  </verify>
  <acceptance_criteria>
    - Zero directories matching agent-* in /Users/claw2501/services/smithers/.claude/worktrees/
    - No error during removal
  </acceptance_criteria>
  <done>All 9 stale agent worktrees removed from services/smithers, disk space reclaimed</done>
</task>

</tasks>

<verification>
- git log --oneline works in each Extreme Pro service directory
- git show --stat HEAD shows no .env files committed
- No agent-* worktrees remain in services/smithers/.claude/worktrees/
</verification>

<success_criteria>
Three critical services on Extreme Pro are version-controlled. Nine stale agent worktrees removed. No secrets committed to any repo.
</success_criteria>

<output>
After completion, create `.planning/phases/08-system-consolidation-security-hardening/08-03-SUMMARY.md`
</output>
