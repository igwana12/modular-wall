---
phase: 08-system-consolidation-security-hardening
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified: []
autonomous: false
requirements: [SEC-06]

must_haves:
  truths:
    - "All services still running after consolidation changes"
    - "All cron jobs intact"
    - "All MCP servers responding"
    - "All skills accessible via Claude Code"
    - "Smithers manifest refreshed and valid"
    - "No regressions introduced by security hardening"
  artifacts:
    - path: "/Users/claw2501/.planning/phases/08-system-consolidation-security-hardening/08-05-SUMMARY.md"
      provides: "Verification results"
  key_links:
    - from: "Smithers port 8200"
      to: "health endpoint"
      via: "HTTP GET"
      pattern: "localhost:8200/health"
    - from: "LLM Router port 8100"
      to: "models endpoint"
      via: "HTTP GET"
      pattern: "localhost:8100/models"
---

<objective>
Verify that all consolidation changes (Plans 01-04) haven't broken anything. Check every service, cron job, MCP server, and skill is still functioning. This is the post-consolidation smoke test.

Purpose: Changes across credentials, skills, git repos, worktrees, and Smithers config could have cascading effects. Verify everything before considering the phase complete.
Output: Verification report confirming all systems operational.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-system-consolidation-security-hardening/08-CONTEXT.md
@.planning/phases/08-system-consolidation-security-hardening/08-01-SUMMARY.md
@.planning/phases/08-system-consolidation-security-hardening/08-02-SUMMARY.md
@.planning/phases/08-system-consolidation-security-hardening/08-03-SUMMARY.md
@.planning/phases/08-system-consolidation-security-hardening/08-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify all services, cron jobs, and MCP servers</name>
  <files></files>
  <read_first>
    /Users/claw2501/services/smithers/manifest.py
    /Users/claw2501/Library/LaunchAgents/com.claw.smithers.plist
    /Users/claw2501/Library/LaunchAgents/com.claw.llm-router.plist
  </read_first>
  <action>
1. Check all services are responding:
   - curl -s http://localhost:8200/health (Smithers)
   - curl -s http://localhost:8100/models (LLM Router)
   - curl -s http://localhost:8300/health (orb-backend) — if running
   - curl -s http://localhost:5555/api/scripts (JARVIS) — if running
   - curl -s http://localhost:8000/health (Sacred Circuits) — if running
   - curl -s http://localhost:11434/api/tags (Ollama) — if running

   For each: record HTTP status code and whether it returned valid JSON/response.
   Services that are not expected to be running (optional) should be noted but not flagged as failures.

2. Check LaunchAgents are loaded:
   launchctl list | grep claw
   Verify com.claw.smithers and com.claw.llm-router are in the list.

3. Check cron jobs:
   crontab -l 2>/dev/null
   Record all entries. Verify none reference deleted/moved paths.

4. Check MCP servers responding:
   - List MCP config: cat ~/.claude/claude_desktop_config.json 2>/dev/null || cat ~/Library/Application\ Support/Claude/claude_desktop_config.json 2>/dev/null
   - For each MCP server defined, verify the binary/script exists at the specified path

5. Verify Smithers manifest builds:
   cd /Users/claw2501/services/smithers && python3 -c "from manifest import scan_skills, build_manifest; m = build_manifest(); print(f'Services: {len(m.get(\"services\", []))}, Skills: {len(m.get(\"skills\", []))}')"

6. Verify skills are accessible:
   ls ~/.claude/skills/ | wc -l
   Confirm count matches expected post-consolidation number from Plan 02 summary.

7. Verify no broken symlinks remain:
   find ~/.claude/skills/ -type l ! -exec test -e {} \; -print | wc -l
   Must be 0.

8. Quick credential check — verify load-keys.sh still works:
   source "/Volumes/Extreme Pro/CONFIG/load-keys.sh" && echo "Keys loaded: $(env | grep -c '_KEY\|_TOKEN\|_SECRET')"

9. Compile results into a structured report.
  </action>
  <verify>
    <automated>bash -c 'curl -sf http://localhost:8200/health > /dev/null && curl -sf http://localhost:8100/models > /dev/null && echo "PASS: Core services responding" || echo "FAIL: Service(s) down"'</automated>
  </verify>
  <acceptance_criteria>
    - Smithers (8200) returns 200 on /health
    - LLM Router (8100) returns 200 on /models
    - LaunchAgents for smithers and llm-router are loaded
    - No broken symlinks in ~/.claude/skills/
    - Smithers manifest builds without error
    - load-keys.sh exports keys successfully
  </acceptance_criteria>
  <done>All core services verified running, cron jobs intact, MCP servers accessible, skills clean, credentials working</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User confirms system health post-consolidation</name>
  <files></files>
  <read_first></read_first>
  <action>
Present the verification report from Task 1 to the user. Highlight any warnings or failures. Ask user to confirm the following manual checks:
1. Open Slack and confirm Smithers bot is responding in channels
2. Try a Claude Code command that uses skills (e.g., list available skills)
3. Confirm external drives (Extreme Pro, AI_WORKSPACE) are still accessible
4. If using Hyperliquid trading bot, verify it still connects (check metasignals-bot logs)
  </action>
  <verify>
    <automated>echo "CHECKPOINT: Requires user confirmation"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms "approved" — all systems operational
    - Or user reports specific issues for remediation
  </acceptance_criteria>
  <done>User has confirmed all systems operational post-consolidation, or issues have been identified for remediation</done>
</task>

</tasks>

<verification>
- Smithers responds at :8200
- LLM Router responds at :8100
- LaunchAgents loaded
- No broken skill symlinks
- Manifest builds with custom skill activation data
- User confirms system health
</verification>

<success_criteria>
All services running, all cron jobs intact, all MCP servers responding, all skills accessible, Smithers manifest valid with custom skill routing, user confirms no regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-system-consolidation-security-hardening/08-05-SUMMARY.md`
</output>
