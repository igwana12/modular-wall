---
phase: 08-system-consolidation-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Volumes/Extreme Pro/CONFIG/sync-keys.sh
  - /Volumes/Extreme Pro/CONFIG/load-keys.sh
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "All API keys consolidated into one master wallet"
    - "Services load keys from canonical source, not scattered .env files"
    - "sync-keys.sh audited and updated post-incident"
    - "No plaintext wallet keys in unprotected .env files on external drives"
  artifacts:
    - path: "/Volumes/Extreme Pro/CONFIG/sync-keys.sh"
      provides: "Audited key sync script"
      contains: "sync_file"
    - path: "/Volumes/Extreme Pro/CONFIG/load-keys.sh"
      provides: "Single-source key loader for services"
      contains: "source.*WALLET"
  key_links:
    - from: "/Volumes/Extreme Pro/CONFIG/load-keys.sh"
      to: "/Volumes/Extreme Pro/CONFIG/API_KEYS_WALLET.env"
      via: "source command"
      pattern: "source.*API_KEYS_WALLET"
---

<objective>
Audit and harden the credential management system. The sync-keys.sh script was updated 4 days after a credential theft incident (Mar 22 -> Mar 26) and needs verification. Create a single canonical load-keys.sh that services can source. Audit all .env files on external drives and ensure no stale/compromised keys remain.

Purpose: Eliminate scattered plaintext credentials — the #1 security risk identified in the audit, especially critical given the Mar 22 security incident where all API keys were compromised.
Output: Hardened sync-keys.sh, new load-keys.sh canonical loader, audit report of all .env files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-system-consolidation-security-hardening/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit sync-keys.sh and all .env files across drives</name>
  <files>/Volumes/Extreme Pro/CONFIG/sync-keys.sh</files>
  <read_first>
    /Volumes/Extreme Pro/CONFIG/sync-keys.sh
    /Volumes/Extreme Pro/CONFIG/API_KEYS_WALLET.env
    /Users/claw2501/services/smithers/.env
    /Users/claw2501/services/llm-router/.env
    /Volumes/AI_WORKSPACE/Trading/metasignals-bot/.env
  </read_first>
  <action>
1. Read sync-keys.sh fully. Verify it was updated post-incident (Mar 26). Document which keys it syncs to which files.

2. Scan all .env files on both external drives:
   - find "/Volumes/Extreme Pro" -name ".env" -type f 2>/dev/null
   - find "/Volumes/AI_WORKSPACE" -name ".env" -type f 2>/dev/null

3. For each .env file found, record:
   - Path
   - Last modified date
   - Which keys it contains (key names only, NOT values)
   - Whether it's in sync-keys.sh target list

4. Check for dangerous patterns:
   - HL_API_WALLET_KEY or HYPERLIQUID_API_KEY in any .env outside the master wallet
   - Any keys that were NOT rotated post-incident (compare wallet dates)
   - Any .env files NOT covered by sync-keys.sh

5. Update sync-keys.sh:
   - Add any missing .env targets that should be synced
   - Remove any targets pointing to files that no longer exist
   - Add a header comment documenting the last audit date
   - Add safety check: refuse to run if wallet file is older than any target .env (prevents overwriting newer keys with stale wallet)

6. Create an audit report at /Volumes/Extreme Pro/CONFIG/credential-audit-2026-03-28.txt documenting findings.
  </action>
  <verify>
    <automated>bash -c 'test -f "/Volumes/Extreme Pro/CONFIG/credential-audit-2026-03-28.txt" && grep -q "AUDIT DATE" "/Volumes/Extreme Pro/CONFIG/credential-audit-2026-03-28.txt" && echo "PASS" || echo "FAIL"'</automated>
  </verify>
  <acceptance_criteria>
    - credential-audit-2026-03-28.txt exists with findings
    - sync-keys.sh has updated audit date header
    - All .env files on both drives catalogued
  </acceptance_criteria>
  <done>Every .env file on external drives catalogued, sync-keys.sh audited and updated, dangerous key patterns flagged</done>
</task>

<task type="auto">
  <name>Task 2: Create canonical load-keys.sh and harden trading bot credentials</name>
  <files>/Volumes/Extreme Pro/CONFIG/load-keys.sh</files>
  <read_first>
    /Volumes/Extreme Pro/CONFIG/sync-keys.sh
    /Volumes/Extreme Pro/CONFIG/API_KEYS_WALLET.env
    /Volumes/AI_WORKSPACE/Trading/metasignals-bot/.env
  </read_first>
  <action>
1. Create /Volumes/Extreme Pro/CONFIG/load-keys.sh — a script services can source:
   ```bash
   #!/bin/bash
   # Canonical key loader — source this instead of maintaining per-service .env
   # Usage: source "/Volumes/Extreme Pro/CONFIG/load-keys.sh"
   WALLET="/Volumes/Extreme Pro/CONFIG/API_KEYS_WALLET.env"
   if [ ! -f "$WALLET" ]; then
     echo "[load-keys] ERROR: Wallet not found at $WALLET" >&2
     echo "[load-keys] Is Extreme Pro mounted?" >&2
     return 1 2>/dev/null || exit 1
   fi
   set -a
   source "$WALLET"
   set +a
   echo "[load-keys] Loaded $(grep -c '=' "$WALLET") keys from wallet"
   ```

2. For the Hyperliquid trading bot at /Volumes/AI_WORKSPACE/Trading/metasignals-bot/.env:
   - This has REAL wallet keys (HL_API_WALLET_KEY) in plaintext
   - Add this path to sync-keys.sh if not already present
   - Ensure the wallet key is in the master wallet
   - Add a comment warning in the .env: "# WARNING: Contains real wallet keys — sourced from master wallet via sync-keys.sh"

3. chmod 700 load-keys.sh (owner-only execute)
4. chmod 700 sync-keys.sh (ensure owner-only)
  </action>
  <verify>
    <automated>bash -c 'test -x "/Volumes/Extreme Pro/CONFIG/load-keys.sh" && source "/Volumes/Extreme Pro/CONFIG/load-keys.sh" && [ -n "$ANTHROPIC_API_KEY" ] && echo "PASS" || echo "FAIL"'</automated>
  </verify>
  <acceptance_criteria>
    - load-keys.sh exists and is executable (chmod 700)
    - Sourcing load-keys.sh exports all wallet keys into the shell
    - sync-keys.sh is also chmod 700
    - Trading bot .env path is in sync-keys.sh target list
  </acceptance_criteria>
  <done>Canonical key loader created, trading bot credentials covered by sync pipeline, file permissions hardened</done>
</task>

</tasks>

<verification>
- source "/Volumes/Extreme Pro/CONFIG/load-keys.sh" succeeds and exports keys
- sync-keys.sh targets all known .env files including trading bot
- No plaintext wallet keys exist outside the master wallet and its synced targets
- File permissions are 700 on both scripts
</verification>

<success_criteria>
All credentials consolidated into single master wallet with canonical loader. Sync script audited post-incident. Trading bot credentials integrated into sync pipeline.
</success_criteria>

<output>
After completion, create `.planning/phases/08-system-consolidation-security-hardening/08-01-SUMMARY.md`
</output>
