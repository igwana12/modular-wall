---
phase: 03-physical-cards-market-launch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/oracle/src/app/page.tsx
  - apps/oracle/src/app/oracle/page.tsx
  - apps/oracle/src/app/reserved/page.tsx
  - apps/oracle/src/app/layout.tsx
  - apps/oracle/src/components/landing-hero.tsx
  - apps/oracle/src/components/landing-features.tsx
  - apps/oracle/src/components/deposit-cta.tsx
autonomous: true
requirements: [LAUNCH-01, LAUNCH-04]
must_haves:
  truths:
    - "Visitor sees a compelling landing page at the root URL with value proposition"
    - "Visitor can enter email and click to reserve with $1 deposit"
    - "After successful deposit, visitor sees a confirmation page"
    - "Reading completions are tracked with Vercel Analytics events"
  artifacts:
    - path: "apps/oracle/src/app/page.tsx"
      provides: "Landing page at root URL"
      min_lines: 50
    - path: "apps/oracle/src/app/layout.tsx"
      provides: "Landing-specific layout without app chrome"
      min_lines: 10
    - path: "apps/oracle/src/app/reserved/page.tsx"
      provides: "Post-deposit confirmation page"
      min_lines: 20
    - path: "apps/oracle/src/components/deposit-cta.tsx"
      provides: "Email input + deposit button component"
      contains: "api/deposit"
  key_links:
    - from: "apps/oracle/src/components/deposit-cta.tsx"
      to: "/api/deposit"
      via: "fetch POST with email"
      pattern: "fetch.*api/deposit"
    - from: "apps/oracle/src/app/layout.tsx"
      to: "@vercel/analytics"
      via: "Analytics component in root layout"
      pattern: "Analytics"
---

<objective>
Build the oracleball.ai landing page with email capture, $1 deposit CTA, confirmation page, and Vercel Analytics for revenue tracking.

Purpose: The landing page is the front door for Oracle Cards (LAUNCH-01). Conversion tracking validates revenue (LAUNCH-04). The deposit CTA connects to the /api/deposit route from Plan 01.

Output: Landing page route group, deposit CTA component, reserved confirmation page, and analytics integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-physical-cards-market-launch/03-RESEARCH.md

@apps/oracle/src/app/page.tsx
@apps/oracle/src/app/layout.tsx
@apps/oracle/src/components/daily-card.tsx
@apps/oracle/src/types/deity.ts

<interfaces>
From apps/oracle/src/app/page.tsx:
```typescript
// Current homepage — has DailyCard, DeityGallery, ReadingCounter
// Will be moved to /oracle route; landing page takes root
import { DailyCard } from "@/components/daily-card";
import { DeityGallery } from "@/components/deity-gallery";
import { ReadingCounter } from "@/components/reading-counter";
```

Deposit API (from Plan 01):
```typescript
// POST /api/deposit
// Body: { email?: string }
// Response: { url: string } — Stripe Checkout URL
```

All 21 deity names and IDs available from services/orb-backend/gods/*.json
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landing page with deposit CTA and route restructuring</name>
  <files>apps/oracle/src/app/page.tsx, apps/oracle/src/app/oracle/page.tsx, apps/oracle/src/components/landing-hero.tsx, apps/oracle/src/components/landing-features.tsx, apps/oracle/src/components/deposit-cta.tsx</files>
  <read_first>apps/oracle/src/app/page.tsx, apps/oracle/src/app/layout.tsx, apps/oracle/AGENTS.md, node_modules/next/dist/docs/</read_first>
  <action>
1. Check `node_modules/next/dist/docs/` for Next.js 16 route group behavior before restructuring.

2. Restructure routes — move existing homepage to `/oracle`, make landing page the new root:
   - Move existing `apps/oracle/src/app/page.tsx` to `apps/oracle/src/app/oracle/page.tsx` (preserves daily-card, deity-gallery, reading-counter)
   - Create new `apps/oracle/src/app/page.tsx` as the public landing page
   - No route groups needed — simple named route approach

3. Create `apps/oracle/src/components/landing-hero.tsx` (client component):
   - Full-viewport hero section with dark, mystical theme
   - Headline: "Oracle Cards" with subheading about AI-powered Greek deity readings
   - Brief value proposition: "21 Greek gods. Personalized AI readings. Physical cards with digital magic."
   - Use Tailwind for styling, consistent with existing shadcn base-nova theme
   - Include a subtle animation (opacity fade-in, not heavy — motion library already installed)

4. Create `apps/oracle/src/components/landing-features.tsx`:
   - 3 feature cards: (1) "Physical Cards" — PANTHEON art, premium print, (2) "AI Oracle Readings" — personalized deity voice, (3) "Sacred Circuits" — mythology meets technology
   - Use shadcn card patterns from existing components
   - Responsive: 3-col on desktop, 1-col on mobile

5. Create `apps/oracle/src/components/deposit-cta.tsx` ("use client"):
   - Email input field (type="email", placeholder "Enter your email")
   - Button: "Reserve Early Access — $1" (styled prominently)
   - Below button: small text "Fully refundable deposit. Secures early-bird pricing."
   - On submit: POST to `/api/deposit` with { email }, then redirect to session.url (Stripe Checkout)
   - Loading state on button during fetch
   - Error handling with user-visible message
   - Also include a secondary "Just sign up for updates" link/button (email-only, no deposit — for LAUNCH-02 email list growth)

6. Compose the new root `page.tsx` (landing page):
   - Import LandingHero, LandingFeatures, DepositCta
   - Stack vertically: Hero -> Features -> CTA section -> Footer with links
   - Footer: links to /oracle, /guidebook, /terms
   - Add metadata export: title "Oracle Cards — AI-Powered Greek Deity Readings", description for SEO
  </action>
  <verify>
    <automated>test -f apps/oracle/src/app/\(landing\)/page.tsx && grep -l "api/deposit" apps/oracle/src/components/deposit-cta.tsx && grep -l "refundable" apps/oracle/src/components/deposit-cta.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Landing page exists at apps/oracle/src/app/page.tsx
    - Landing page is at root / — existing oracle homepage moved to /oracle
    - Existing homepage content preserved at /oracle route
    - deposit-cta.tsx POSTs to /api/deposit — grep: api/deposit
    - "Fully refundable" language visible to users — grep: refundable
    - Email input field present — grep: type.*email
    - SEO metadata exported from landing page
  </acceptance_criteria>
  <done>Landing page renders at root URL with hero, features, and $1 deposit CTA. Existing oracle app accessible at /oracle. Refund language visible.</done>
</task>

<task type="auto">
  <name>Task 2: Reservation confirmation page and Vercel Analytics</name>
  <files>apps/oracle/src/app/reserved/page.tsx, apps/oracle/src/app/layout.tsx</files>
  <read_first>apps/oracle/src/app/layout.tsx, apps/oracle/package.json</read_first>
  <action>
1. Install Vercel Analytics: `cd apps/oracle && npm install @vercel/analytics`

2. Create `apps/oracle/src/app/reserved/page.tsx`:
   - Confirmation page shown after successful $1 deposit
   - Reads `session_id` from searchParams (URL: /reserved?session_id=...)
   - Display: checkmark icon, "You're in!" heading, "Your $1 deposit secures early-bird pricing for Oracle Cards."
   - Sub-text: "Check your email for what's next." (anticipates Kit email sequence from Plan 03)
   - CTA buttons: "Try a Free Reading" linking to /oracle, "Share with Friends" (copy link)
   - Dark, mystical theme matching landing page
   - Export metadata: title "Reservation Confirmed — Oracle Cards"

3. Add Vercel Analytics to root layout (`apps/oracle/src/app/layout.tsx`):
   - Import `Analytics` from `@vercel/analytics/react`
   - Add `<Analytics />` inside the root layout body, after children
   - This tracks page views automatically across all routes

4. Add reading completion tracking event to the reading page.
   - Find the reading completion handler in the reading flow (likely in the SSE stream completion callback)
   - Add: `import { track } from '@vercel/analytics'` and call `track('reading_completed', { deity: deityId, tier: userTier })` when a reading finishes
   - This directly supports LAUNCH-04 (first 100 paid readings metric)

Note: The reading page is at apps/oracle/src/app/read/[deity_id]/page.tsx — read it to find where reading completion is detected, then add the tracking event there or in the relevant client component.
  </action>
  <verify>
    <automated>test -f apps/oracle/src/app/reserved/page.tsx && grep -l "Analytics" apps/oracle/src/app/layout.tsx && grep -l "reading_completed" apps/oracle/src/app/read/\[deity_id\]/*.tsx 2>/dev/null || grep -rl "reading_completed" apps/oracle/src/components/</automated>
  </verify>
  <acceptance_criteria>
    - Reserved confirmation page exists at apps/oracle/src/app/reserved/page.tsx
    - Page reads session_id from URL params — grep: session_id
    - Vercel Analytics component added to root layout — grep: Analytics.*@vercel/analytics
    - reading_completed tracking event fires on reading completion — grep: reading_completed
    - Track event includes deity and tier metadata — grep: deity.*tier
  </acceptance_criteria>
  <done>Confirmation page shows after deposit. Vercel Analytics tracks all page views. Reading completions tracked with deity and tier metadata for LAUNCH-04 revenue validation.</done>
</task>

</tasks>

<verification>
- Landing page renders at root / with hero, features, deposit CTA
- Existing oracle app content accessible at /oracle
- /reserved page renders with confirmation content
- Vercel Analytics component in root layout
- reading_completed event tracked
- `cd apps/oracle && npm run build` succeeds
</verification>

<success_criteria>
- Landing page is the front door with email + $1 deposit flow
- Post-deposit confirmation page exists with next-step messaging
- Analytics tracking in place for page views and reading completions
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-physical-cards-market-launch/03-02-SUMMARY.md`
</output>
