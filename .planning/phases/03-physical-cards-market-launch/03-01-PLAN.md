---
phase: 03-physical-cards-market-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate-qr-codes.ts
  - apps/oracle/src/app/api/deposit/route.ts
  - apps/oracle/src/app/api/webhooks/stripe/route.ts
  - assets/qr-codes/
autonomous: true
requirements: [CARD-02, LAUNCH-03]
must_haves:
  truths:
    - "21 SVG QR codes exist, one per deity, each encoding https://oracleball.ai/read/{deity_id}"
    - "$1 deposit checkout session creates successfully via POST /api/deposit"
    - "Stripe webhook handles checkout.session.completed for reservation_deposit metadata"
  artifacts:
    - path: "scripts/generate-qr-codes.ts"
      provides: "QR code generation for all 21 deities"
      contains: "errorCorrectionLevel.*H"
    - path: "assets/qr-codes/zeus.svg"
      provides: "Sample QR SVG output"
    - path: "apps/oracle/src/app/api/deposit/route.ts"
      provides: "$1 deposit Stripe Checkout session creation"
      exports: ["POST"]
    - path: "apps/oracle/src/app/api/webhooks/stripe/route.ts"
      provides: "Extended webhook handling for deposit events"
      contains: "reservation_deposit"
  key_links:
    - from: "scripts/generate-qr-codes.ts"
      to: "services/orb-backend/gods/*.json"
      via: "reads deity ID from each JSON config"
      pattern: "god\\.id"
    - from: "apps/oracle/src/app/api/deposit/route.ts"
      to: "apps/oracle/src/lib/stripe.ts"
      via: "import stripe client"
      pattern: "import.*stripe.*from.*@/lib/stripe"
---

<objective>
Generate print-ready QR codes for all 21 Oracle Cards and build the $1 reservation deposit payment flow.

Purpose: QR codes are the bridge between physical cards and digital readings (CARD-02). The deposit API enables the 30x-conversion reservation system (LAUNCH-03). Both are prerequisites for the landing page and card design.

Output: 21 SVG QR files in assets/qr-codes/, a working /api/deposit endpoint, and an extended Stripe webhook handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-physical-cards-market-launch/03-RESEARCH.md

@apps/oracle/src/lib/stripe.ts
@apps/oracle/src/app/api/webhooks/stripe/route.ts
@apps/oracle/src/app/api/checkout/route.ts
@services/orb-backend/gods/zeus.json

<interfaces>
From apps/oracle/src/lib/stripe.ts:
```typescript
import Stripe from "stripe";
export const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!, { typescript: true });
```

From services/orb-backend/gods/zeus.json (sample deity config):
```json
{
  "id": "zeus",
  "name": "Zeus",
  "title": "King of the Gods",
  "color_palette": ["#ffffff", "#ffd600", "#87ceeb"]
}
```

All 21 deity IDs: aphrodite, apollo, ares, artemis, athena, demeter, dionysus, eros, hades, hecate, hephaestus, hera, hermes, hestia, nike, pan, persephone, poseidon, prometheus, tyche, zeus
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: QR code generation script for all 21 deities</name>
  <files>scripts/generate-qr-codes.ts, assets/qr-codes/</files>
  <read_first>services/orb-backend/gods/zeus.json, apps/oracle/package.json</read_first>
  <action>
1. Install qrcode and types: `cd apps/oracle && npm install qrcode @types/qrcode`

2. Create `scripts/generate-qr-codes.ts` that:
   - Reads all 21 JSON files from `services/orb-backend/gods/` directory
   - For each deity, parses the JSON and extracts `id` and `name` fields
   - Generates SVG QR code via `QRCode.toFile()` with these exact options:
     - URL: `https://oracleball.ai/read/${god.id}` (per CARD-02 — permanent URLs)
     - errorCorrectionLevel: 'H' (per locked decision — highest for small card print)
     - type: 'svg' (vector for infinite resolution print — NEVER PNG)
     - margin: 2 (quiet zone)
     - width: 300 (pixels, but SVG scales infinitely)
     - color: dark '#000000', light '#ffffff' (maximum contrast per research)
   - Creates `assets/qr-codes/` output directory if it doesn't exist (use `mkdir -p` equivalent)
   - Outputs `assets/qr-codes/{deity_id}.svg` for each deity
   - Logs each generated QR with deity name and URL

3. Add a package.json script in the project root: `"generate:qr": "npx tsx scripts/generate-qr-codes.ts"`

4. Run the script and verify 21 SVG files are generated.

Note: The `qrcode` npm package is installed in apps/oracle/ but the script runs from project root. Use relative paths to services/orb-backend/gods/ and output to assets/qr-codes/.
  </action>
  <verify>
    <automated>ls assets/qr-codes/*.svg | wc -l | grep 21 && grep -l "oracleball.ai/read/zeus" assets/qr-codes/zeus.svg</automated>
  </verify>
  <acceptance_criteria>
    - 21 SVG files exist in assets/qr-codes/, one per deity ID
    - Each SVG contains the correct URL (oracleball.ai/read/{deity_id})
    - Script uses errorCorrectionLevel 'H' (grep: errorCorrectionLevel.*H)
    - SVG format used (NOT PNG) — grep: type.*svg
  </acceptance_criteria>
  <done>21 print-ready SVG QR codes generated, each encoding the permanent deity reading URL at Error Correction Level H</done>
</task>

<task type="auto">
  <name>Task 2: $1 deposit API route and webhook extension</name>
  <files>apps/oracle/src/app/api/deposit/route.ts, apps/oracle/src/app/api/webhooks/stripe/route.ts</files>
  <read_first>apps/oracle/src/app/api/webhooks/stripe/route.ts, apps/oracle/src/app/api/checkout/route.ts, apps/oracle/src/lib/stripe.ts</read_first>
  <action>
1. Check `node_modules/next/dist/docs/` for any Next.js 16 breaking changes to API routes before writing code (per AGENTS.md warning).

2. Create `apps/oracle/src/app/api/deposit/route.ts`:
   - Export async POST handler
   - Parse request body for optional `email` field: `const { email } = await req.json()`
   - Create Stripe Checkout Session with:
     - mode: 'payment' (one-time, NOT subscription)
     - line_items: single item, price_data with currency 'usd', unit_amount 100 ($1.00), product_data name "Oracle Cards - Early Access Reservation", description "Fully refundable $1 deposit. Secures early-bird pricing."
     - customer_email: email || undefined
     - success_url: `${process.env.NEXT_PUBLIC_APP_URL}/reserved?session_id={CHECKOUT_SESSION_ID}`
     - cancel_url: `${process.env.NEXT_PUBLIC_APP_URL}/?cancelled=true`
     - metadata: { type: 'reservation_deposit' }
   - Return JSON: `{ url: session.url }`
   - Handle errors with 500 response

3. Extend `apps/oracle/src/app/api/webhooks/stripe/route.ts`:
   - In the `checkout.session.completed` case, BEFORE the existing subscription handling, add a check for `session.metadata?.type === 'reservation_deposit'`
   - When deposit detected:
     - Log: `[Stripe] Reservation deposit received from ${email}`
     - Do NOT call updateUserTier (deposits are not subscriptions)
     - Break out of the case (skip subscription logic)
   - Keep ALL existing subscription handling intact — the deposit check is an early-return guard

4. Do NOT use Stripe Payment Links (per research anti-pattern). Use Checkout Sessions via API for full control.
  </action>
  <verify>
    <automated>grep -l "reservation_deposit" apps/oracle/src/app/api/deposit/route.ts apps/oracle/src/app/api/webhooks/stripe/route.ts && grep "unit_amount.*100" apps/oracle/src/app/api/deposit/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - POST /api/deposit route exists with Stripe Checkout session creation
    - unit_amount is 100 (cents = $1.00) — grep: unit_amount.*100
    - mode is 'payment' not 'subscription' — grep: mode.*payment
    - metadata includes type: 'reservation_deposit' — grep: reservation_deposit
    - Webhook handler checks for reservation_deposit metadata before subscription logic
    - success_url points to /reserved route
    - "Fully refundable" language present in product description
  </acceptance_criteria>
  <done>$1 deposit creates a Stripe Checkout session. Webhook correctly distinguishes deposits from subscriptions. Refund language included in product description.</done>
</task>

</tasks>

<verification>
- 21 SVG QR codes in assets/qr-codes/ with correct URLs
- /api/deposit POST returns a Stripe Checkout URL for $1 payment
- Webhook handler distinguishes deposits from subscriptions without breaking existing subscription flow
- `cd apps/oracle && npm run build` succeeds (no type errors)
</verification>

<success_criteria>
- All 21 QR SVGs generated with Level H error correction
- Deposit API creates Stripe Checkout for $1.00 one-time payment
- Existing subscription webhook flow unchanged
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-physical-cards-market-launch/03-01-SUMMARY.md`
</output>
