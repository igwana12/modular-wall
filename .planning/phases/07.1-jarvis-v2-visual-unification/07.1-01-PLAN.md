---
phase: 07.1-jarvis-v2-visual-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/services/jarvis-visualizer/script.js
  - ~/services/jarvis-visualizer/gpu-particle-shaders.js
  - ~/services/jarvis-visualizer/filmic-tone-system.js
  - ~/services/jarvis-visualizer/effect-composer.js
  - ~/services/jarvis-visualizer/liquid-blob.js
  - ~/.openclaw/workspace/jarvis-voice-v2/frontend/js/audio-engine.js
autonomous: true
requirements: [VIZ-03, VIZ-04, VIZ-05]

must_haves:
  truths:
    - "GPU particle shaders render around the ferrofluid blob without crashes"
    - "Filmic tone post-processing applies cinematic color grading to the scene"
    - "TTS bass/mid/high frequency bands drive distinct blob deformation axes beyond simple volume scaling"
  artifacts:
    - path: "~/services/jarvis-visualizer/script.js"
      provides: "Re-enabled BlobShaderSystem and FilmicToneSystem initialization"
      contains: "new BlobShaderSystem"
    - path: "~/services/jarvis-visualizer/gpu-particle-shaders.js"
      provides: "GPU-accelerated blob shader materials"
      contains: "BlobShaderSystem"
    - path: "~/services/jarvis-visualizer/filmic-tone-system.js"
      provides: "Post-processing filmic tone with EffectComposer"
      contains: "FilmicToneSystem"
    - path: "~/services/jarvis-visualizer/liquid-blob.js"
      provides: "Multi-band voice deformation on blob geometry"
      contains: "createVoiceRipple"
  key_links:
    - from: "~/services/jarvis-visualizer/script.js"
      to: "gpu-particle-shaders.js"
      via: "new BlobShaderSystem(this) in init"
      pattern: "this\\.gpuParticleSystem\\s*=\\s*new BlobShaderSystem"
    - from: "~/services/jarvis-visualizer/script.js"
      to: "filmic-tone-system.js"
      via: "new FilmicToneSystem(renderer, scene, camera) in init"
      pattern: "new FilmicToneSystem"
    - from: "~/.openclaw/workspace/jarvis-voice-v2/frontend/index.html"
      to: "~/services/jarvis-visualizer/liquid-blob.js"
      via: "postMessage from parent to iframe with ttsBass/ttsMid/ttsHigh"
      pattern: "postMessage.*ttsBass"
---

<objective>
Re-enable the two disabled visual systems (GPU particle shaders, filmic tone) and wire multi-band TTS audio into blob deformation so the ferrofluid responds to voice pitch/tone/cadence — not just volume.

Purpose: The user has asked multiple times to "show me features working" and wants blob deformation driven by "voice pitch, tone, cadence" not just energy. Two major systems are commented out in ARTEF4KT that need debugging and re-activation.

Output: GPU shaders rendering, filmic tone active, blob deforming distinctly to bass/mid/high frequency bands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@~/services/jarvis-visualizer/script.js
@~/services/jarvis-visualizer/gpu-particle-shaders.js
@~/services/jarvis-visualizer/filmic-tone-system.js
@~/services/jarvis-visualizer/effect-composer.js
@~/services/jarvis-visualizer/liquid-blob.js
@~/.openclaw/workspace/jarvis-voice-v2/frontend/js/audio-engine.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-enable GPU particle shaders and filmic tone in ARTEF4KT</name>
  <files>
    ~/services/jarvis-visualizer/script.js
    ~/services/jarvis-visualizer/gpu-particle-shaders.js
    ~/services/jarvis-visualizer/filmic-tone-system.js
    ~/services/jarvis-visualizer/effect-composer.js
  </files>
  <action>
In script.js around line 452-456, the BlobShaderSystem is commented out with "temporarily disabled for debugging". Uncomment and re-enable:

1. Replace `// this.gpuParticleSystem = new BlobShaderSystem(this);` with the actual instantiation. Wrap in try/catch with a fallback message but DO NOT swallow the error silently — log the full stack trace so debugging is possible.

2. The FilmicToneSystem (filmic-tone-system.js, 1146 lines) is also disabled in initialization. Find where it should be instantiated (likely near the renderer/scene/camera setup in script.js init) and enable it. The FilmicToneSystem constructor takes (renderer, scene, camera). Set `settings.enabled = true` by default instead of false (line 31 of filmic-tone-system.js).

3. In the render loop (around line 6166), the GPU particle update is guarded by `if (this.gpuParticleSystem)` which is correct — ensure this path executes now that the system is instantiated.

4. Check if effect-composer.js (330 lines, dependency for filmic tone) is loaded in the HTML. If not, add the script tag in index.html before filmic-tone-system.js.

5. The filmic tone system checks `typeof THREE.EffectComposer !== 'undefined'` — verify that Three.js addons (EffectComposer, RenderPass, ShaderPass) are available. If they're bundled in three.min.js, fine. If not, the FilmicToneSystem has a fallback mode that adjusts renderer.toneMapping directly — ensure that fallback path works correctly.

CRITICAL: If the BlobShaderSystem crashes on init (which is why it was disabled), diagnose the actual error. Common causes: missing Three.js uniforms, WebGL2 context issues, or shader compilation failures. Fix the root cause rather than re-disabling.
  </action>
  <verify>
    <automated>Open ~/services/jarvis-visualizer/index.html in browser, check DevTools console for "Initializing Custom Blob Shader System" log without subsequent errors. Verify filmic tone is active by checking renderer.toneMapping is not THREE.NoToneMapping.</automated>
  </verify>
  <done>GPU particle shaders initialize without errors and render visible particles. Filmic tone system applies color grading (visible as subtle contrast/saturation change vs raw render). No console errors related to either system.</done>
</task>

<task type="auto">
  <name>Task 2: Wire multi-band TTS audio to distinct blob deformation axes</name>
  <files>
    ~/services/jarvis-visualizer/liquid-blob.js
    ~/services/jarvis-visualizer/script.js
    ~/.openclaw/workspace/jarvis-voice-v2/frontend/js/audio-engine.js
  </files>
  <action>
Currently the blob responds to a single `energy` value via `createVoiceRipple(energy, angle)`. The user wants "voice pitch, tone, cadence" driving deformation. The audio-engine.js already computes ttsBass, ttsMid, ttsHigh, ttsEnergy — these need to reach the blob.

1. In the JARVIS V2 frontend (parent page), find where audio data is sent to the visualizer iframe via postMessage. Extend the message payload to include `{ ttsBass, ttsMid, ttsHigh, ttsEnergy, spectralCentroid }` from the audio engine.

2. In script.js (ARTEF4KT), find the postMessage listener (should handle messages from parent). Add handling for the multi-band data: store `this._ttsBands = { bass, mid, high, energy, centroid }`.

3. In liquid-blob.js, modify `createVoiceRipple` (or add a new method `applyFrequencyDeformation(bands)`) that maps:
   - **Bass (20-250Hz)**: Drives blob SCALE pulsing (low frequency = body expansion). Scale factor: `1.0 + bass * 0.3`
   - **Mid (250-2000Hz)**: Drives blob surface DISPLACEMENT noise amplitude (voice presence = surface turbulence). Modify the noise amplitude uniform or vertex displacement strength.
   - **High (2000-8000Hz)**: Drives blob SPIKE intensity (sibilance = sharp protrusions). Increase the spike/tentacle effect intensity.
   - **Spectral centroid**: Drives blob COLOR temperature shift (higher centroid = warmer gold, lower = cooler blue-purple).

4. In script.js render loop, call the new multi-band deformation every frame when `this._ttsBands` exists and TTS is active. Use lerp (0.1 factor) for smooth transitions — no abrupt jumps.

5. In audio-engine.js, ensure the TTS analyser node is properly connected and that ttsBass/ttsMid/ttsHigh update every frame during speech. Verify the frequency bin ranges are correct for the three bands.

IMPORTANT: Keep the existing `createVoiceRipple(energy, angle)` working — the new multi-band system augments it, does not replace it. The ripple is an onset event; the deformation is continuous.
  </action>
  <verify>
    <automated>Trigger TTS playback in JARVIS V2. In DevTools console, log `window.ferrofluidVisualizer._ttsBands` — should show non-zero bass/mid/high values during speech. Blob should visibly deform differently for bass-heavy vs sibilant speech.</automated>
  </verify>
  <done>During TTS speech: bass drives scale pulsing, mid drives surface turbulence, high drives spike intensity, spectral centroid shifts color temperature. Blob deformation is visibly multi-dimensional — not just uniform scaling. Silent periods return to calm idle state via lerp decay.</done>
</task>

</tasks>

<verification>
1. Load ARTEF4KT visualizer standalone — GPU particles visible, filmic tone active, no console errors
2. Load JARVIS V2 (parent page with iframe) — trigger speech, observe blob responding with multi-band deformation
3. Compare blob behavior during whispered vs shouted vs sibilant speech — should be visually distinct
</verification>

<success_criteria>
- GPU particle shaders render without crashes (were previously disabled)
- Filmic tone post-processing is active (cinematic color grading visible)
- Blob deforms differently based on bass/mid/high frequency content of TTS audio
- No rendering performance regression (maintain 30+ FPS)
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-jarvis-v2-visual-unification/07.1-01-SUMMARY.md`
</output>
