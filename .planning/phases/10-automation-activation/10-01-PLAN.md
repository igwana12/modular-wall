---
phase: 10-automation-activation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/claw2501/.openclaw/workspace/tools/service-health-check.sh
  - /Users/claw2501/.openclaw/workspace/tools/daily-obsidian-note.sh
  - /Users/claw2501/.openclaw/workspace/tools/morning-briefing-cron.sh
autonomous: true
requirements: [SCHED-01, SCHED-02, SCHED-03]

must_haves:
  truths:
    - "crontab -l shows morning-briefing at 0 7 * * * (7am daily)"
    - "crontab -l shows api-health-check at 0 */6 * * * (every 6 hours)"
    - "crontab -l shows wispr-daily-sync at 0 23 * * * (11pm daily)"
    - "crontab -l shows weekly-improvement at 0 10 * * 0 (Sunday 10am)"
    - "crontab -l shows daily-obsidian-note at 0 6 * * * (6am daily)"
    - "Health check alerts go to Slack #the-orb, not #operations"
    - "Health check only alerts when service is DOWN (unreachable), never for degraded"
  artifacts:
    - path: "/Users/claw2501/.openclaw/workspace/tools/service-health-check.sh"
      provides: "Health check script with correct alert channel and all 9 services"
      contains: "the-orb"
    - path: "/Users/claw2501/.openclaw/workspace/tools/morning-briefing-cron.sh"
      provides: "Cron wrapper that invokes Smithers for morning briefing"
      contains: "localhost:8200"
  key_links:
    - from: "crontab"
      to: "shell scripts"
      via: "cron schedule entries"
      pattern: "morning-briefing-cron.sh"
    - from: "service-health-check.sh"
      to: "Slack #the-orb"
      via: "Smithers execute/v2 API"
      pattern: "the-orb"
---

<objective>
Align all 5 scheduled tasks to their correct cadences per user decisions, fix alert routing to #the-orb, and ensure every cron entry points to a working wrapper script that routes through Smithers Hunter Alpha (FREE tier).

Purpose: This is the activation of the scheduling infrastructure (SCHED-01/02/03). The user wants all 5 tasks running unattended at the right times, with zero cloud billing.
Output: Updated crontab with 5 correct entries, updated scripts with correct alert channels and service lists.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-automation-activation/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix health check script and create morning briefing cron wrapper</name>
  <files>/Users/claw2501/.openclaw/workspace/tools/service-health-check.sh, /Users/claw2501/.openclaw/workspace/tools/morning-briefing-cron.sh</files>
  <read_first>
    /Users/claw2501/.openclaw/workspace/tools/service-health-check.sh
    /Users/claw2501/.claude/scheduled-tasks/morning-briefing/SKILL.md
    /Users/claw2501/.claude/scheduled-tasks/api-health-check/SKILL.md
  </read_first>
  <action>
**service-health-check.sh changes (per D-10, D-11):**
1. Change the Slack alert target from `#operations` (C0AHC4V6ZAT) to `#the-orb`. Find the channel ID for #the-orb by checking existing Slack MCP config or using the Slack API via MCP. Replace every occurrence of the operations channel ID with #the-orb's channel ID.
2. Add missing services to the check list. The SKILL.md defines 7 services + 2 APIs + 3 processes + disk. Current script only checks 4 services + disk + trading bot. Add these missing checks:
   - `check_service "Health Dashboard" "http://localhost:6001"`
   - `check_service "Mission Control" "http://localhost:4000"`
   - `check_service "Podcast Feed" "http://localhost:8877/feed.xml"`
   - orb-backend: `check_service "Orb Backend" "http://localhost:8300"`
   - mirofish process check: `pgrep -f "mirofish"` (per SKILL.md step 3)
3. Keep the "only alert on failures" pattern (per D-11 -- down-only alerts). Do NOT add "all-pass" messages.
4. Ensure the Smithers POST payload mentions `#the-orb` as the target channel.

**morning-briefing-cron.sh (new file):**
Create a wrapper script at `/Users/claw2501/.openclaw/workspace/tools/morning-briefing-cron.sh` that:
1. Sets `export PATH=/opt/homebrew/bin:$PATH`
2. Calls `curl -s -X POST http://localhost:8200/execute/v2 -H "Content-Type: application/json"` with the task payload from the existing crontab morning briefing entry (the full JSON task string describing the Daybreak briefing)
3. Logs output to `/Users/claw2501/.openclaw/workspace/logs/morning-briefing.log`
4. Make it executable: `chmod +x`
  </action>
  <verify>
    <automated>grep -c "the-orb" /Users/claw2501/.openclaw/workspace/tools/service-health-check.sh && grep -c "localhost:6001\|localhost:4000\|localhost:8877\|localhost:8300" /Users/claw2501/.openclaw/workspace/tools/service-health-check.sh && test -x /Users/claw2501/.openclaw/workspace/tools/morning-briefing-cron.sh && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "the-orb" service-health-check.sh` returns at least 1 match (alert channel changed)
    - `grep -c "check_service" service-health-check.sh` returns 8 or more (all services covered)
    - `grep "operations" service-health-check.sh` returns 0 matches (old channel removed)
    - morning-briefing-cron.sh exists, is executable, contains `localhost:8200/execute/v2`
    - morning-briefing-cron.sh contains `morning-briefing.log` as log target
  </acceptance_criteria>
  <done>Health check script monitors all 9 services and alerts to #the-orb only on failures. Morning briefing has a cron wrapper script routing through Smithers.</done>
</task>

<task type="auto">
  <name>Task 2: Update crontab with correct cadences for all 5 tasks</name>
  <files>crontab (system)</files>
  <read_first>
    /Users/claw2501/.openclaw/workspace/tools/service-health-check.sh
    /Users/claw2501/.openclaw/workspace/tools/daily-obsidian-note.sh
    /Users/claw2501/.openclaw/workspace/tools/wispr-sync.sh
    /Users/claw2501/.openclaw/workspace/tools/morning-briefing-cron.sh
  </read_first>
  <action>
Read current crontab with `crontab -l`. Make a backup first: `crontab -l > /tmp/crontab-backup-$(date +%Y%m%d).txt`.

Modify these specific entries (preserve ALL other existing cron entries unchanged):

1. **daily-obsidian-note**: Change `30 6 * * *` to `0 6 * * *` (per D-09: 6am, not 6:30am). Keep same script path.

2. **service-health-check (api-health-check)**: Change `0 7 * * *` to `0 */6 * * *` (per D-06: every 6 hours = 0:00, 6:00, 12:00, 18:00). Keep same script path.

3. **wispr-daily-sync**: Change `0 8 * * *` to `0 23 * * *` (per D-07: 11pm). Keep same script path.

4. **morning-briefing**: Change `30 8 * * *` with the inline curl to `0 7 * * * bash /Users/claw2501/.openclaw/workspace/tools/morning-briefing-cron.sh` (per D-05: 7am). Replace the inline curl with the wrapper script created in Task 1.

5. **weekly-improvement**: Already at `0 10 * * 0` (Sunday 10am per D-08) -- verify it's correct and leave unchanged.

Add a comment block at the top of the automation section:
```
# === PHASE 10: AUTOMATION ACTIVATION (2026-04-02) ===
# All tasks route through Smithers :8200 / Hunter Alpha FREE tier (per D-01, D-04)
# Cadences: D-05 morning 7am, D-06 health 6hr, D-07 wispr 11pm, D-08 weekly Sun10am, D-09 obsidian 6am
```

Write the new crontab with `crontab -` (pipe from variable or temp file).

IMPORTANT: Preserve ALL non-automation entries (trading bot, github backup, security audit, meta-cron, sunday reflection, smithers heartbeat, content generation, trend scan). Only modify the 5 scheduled task entries listed above.
  </action>
  <verify>
    <automated>crontab -l | grep -E "^0 6 \* \* \*.*daily-obsidian" && crontab -l | grep -E "^0 \*/6 \* \* \*.*health" && crontab -l | grep -E "^0 23 \* \* \*.*wispr" && crontab -l | grep -E "^0 7 \* \* \*.*morning-briefing" && crontab -l | grep -E "^0 10 \* \* 0.*improvement" && echo "ALL 5 CADENCES CORRECT"</automated>
  </verify>
  <acceptance_criteria>
    - `crontab -l | grep "0 6 \* \* \*"` shows daily-obsidian-note entry
    - `crontab -l | grep "0 \*/6 \* \* \*"` shows service-health-check entry
    - `crontab -l | grep "0 23 \* \* \*"` shows wispr-sync entry
    - `crontab -l | grep "0 7 \* \* \*"` shows morning-briefing-cron.sh entry (NOT inline curl)
    - `crontab -l | grep "0 10 \* \* 0"` shows weekly-improvement entry
    - `crontab -l | grep "james_crypto"` still shows trading bot (not deleted)
    - `crontab -l | grep "github-backup"` still shows backup (not deleted)
    - Backup file exists at /tmp/crontab-backup-*.txt
  </acceptance_criteria>
  <done>All 5 scheduled tasks have correct cadences in crontab matching user decisions D-05 through D-09. No existing cron jobs were lost.</done>
</task>

<task type="auto">
  <name>Task 3: Dry-run validation and FREE tier routing compliance check</name>
  <files>None (validation only)</files>
  <read_first>
    /Users/claw2501/.openclaw/workspace/tools/service-health-check.sh
    /Users/claw2501/.openclaw/workspace/tools/daily-obsidian-note.sh
    /Users/claw2501/.openclaw/workspace/tools/wispr-sync.sh
    /Users/claw2501/.openclaw/workspace/tools/morning-briefing-cron.sh
    /Volumes/Extreme Pro/ACTIVE/smithers/data/routing_policy.json
  </read_first>
  <action>
Run each script manually to verify it executes without errors:

1. **daily-obsidian-note.sh**: Run it. Since today's note likely exists, it should exit cleanly (exit 0 without creating). Verify with: `echo $?` should be 0.

2. **service-health-check.sh**: Run it. Capture output. It should either:
   - Exit silently (all services up), OR
   - Print failure list and attempt Smithers POST (which is fine -- we're validating the script runs)
   Verify the output mentions `#the-orb` or the correct channel if failures exist.

3. **wispr-sync.sh**: Run with `--stats` flag only (safe, read-only). Verify it can access the Wispr database.

4. **morning-briefing-cron.sh**: Do NOT run (would trigger a real briefing). Instead validate:
   - `bash -n /Users/claw2501/.openclaw/workspace/tools/morning-briefing-cron.sh` (syntax check)
   - `grep "localhost:8200" morning-briefing-cron.sh` (confirms Smithers routing)

5. **weekly-improvement cron entry**: The existing crontab entry is an inline curl. Validate the URL is correct: `curl -s -o /dev/null -w "%{http_code}" http://localhost:8200/status` should return 200 (Smithers is reachable).

6. **FREE tier routing compliance (per D-13, D-14, D-15):**
   Read `/Volumes/Extreme Pro/ACTIVE/smithers/data/routing_policy.json` and verify:
   - Hunter Alpha FREE tier is active and is the default routing tier for automation tasks
   - All 5 scheduled tasks (morning-briefing, api-health-check, wispr-daily-sync, weekly-improvement, daily-obsidian-note) route through Hunter Alpha FREE tier
   - Document this finding in the SUMMARY as the D-13/D-14/D-15 compliance mechanism: "The $5/month ceiling (D-13), protected tasks (D-14), and pause logic (D-15) are satisfied by Hunter Alpha FREE tier routing -- all automation tasks use the FREE tier, so token spend is near-zero by design. No active cost enforcement is needed because the routing layer itself ensures zero cost."
   - If routing_policy.json does NOT confirm FREE tier for these tasks, flag it as a blocker and document what routing tier is configured.

7. **Final crontab listing**: Run `crontab -l` and produce a formatted summary of all 5 automation entries with their cadences for the SUMMARY.

If any script fails syntax check or has a missing dependency, fix it before marking done.
  </action>
  <verify>
    <automated>bash -n /Users/claw2501/.openclaw/workspace/tools/service-health-check.sh && bash -n /Users/claw2501/.openclaw/workspace/tools/daily-obsidian-note.sh && bash -n /Users/claw2501/.openclaw/workspace/tools/morning-briefing-cron.sh && cat "/Volumes/Extreme Pro/ACTIVE/smithers/data/routing_policy.json" | grep -qi "free\|alpha" && echo "ALL SCRIPTS VALID + FREE TIER CONFIRMED"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n service-health-check.sh` exits 0 (valid syntax)
    - `bash -n daily-obsidian-note.sh` exits 0 (valid syntax)
    - `bash -n morning-briefing-cron.sh` exits 0 (valid syntax)
    - `bash -n wispr-sync.sh` exits 0 (valid syntax)
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:8200/status` returns 200
    - `crontab -l` shows exactly 5 automation entries with correct cadences
    - routing_policy.json confirms Hunter Alpha FREE tier is active for automation tasks (D-13/D-14/D-15 compliance documented)
  </acceptance_criteria>
  <done>All 5 scheduled task scripts pass syntax validation, Smithers is reachable, and crontab listing confirms correct cadences. Hunter Alpha FREE tier routing confirmed as D-13/D-14/D-15 compliance mechanism. SCHED-01 (all tasks active), SCHED-02 (morning briefing at 7am), SCHED-03 (health check every 6hr with alerts) are verified.</done>
</task>

</tasks>

<verification>
1. `crontab -l` shows all 5 tasks at correct cadences (D-05 through D-09)
2. No inline curls for tasks that have wrapper scripts
3. Health check script covers all 9 services + trading processes + disk
4. Alert channel is #the-orb everywhere (D-10)
5. No "all-pass" spam messages (D-11)
6. All scripts pass `bash -n` syntax check
7. Smithers at :8200 is reachable
8. routing_policy.json confirms Hunter Alpha FREE tier for all automation (D-13/D-14/D-15)
</verification>

<success_criteria>
- All 5 scheduled tasks verified active in crontab with user-specified cadences
- Morning briefing fires at 7am through Smithers (not cloud /schedule)
- API health check runs every 6 hours and alerts to #the-orb on service failure
- No cloud billing -- everything routes through local Smithers + Hunter Alpha FREE
- All wrapper scripts are executable and pass syntax validation
- D-13/D-14/D-15 cost ceiling compliance documented via FREE tier routing verification
</success_criteria>

<output>
After completion, create `.planning/phases/10-automation-activation/10-01-SUMMARY.md`
</output>
