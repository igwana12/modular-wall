---
phase: 20-pantheon-oracle-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/claw2501/services/jarvis-sandbox/backend/oracle/rag_retriever.py
autonomous: true
requirements:
  - RAG-PATH-01
  - RAG-PATH-02

must_haves:
  truths:
    - "check_rag_ready() returns True when called against ~/.jarvis/myth-vectors"
    - "get_oracle_collection() connects to collection 'myth_notes' without exception"
    - "retrieve_oracle_wisdom() returns non-empty results for a Greek myth query"
  artifacts:
    - path: "/Users/claw2501/services/jarvis-sandbox/backend/oracle/rag_retriever.py"
      provides: "Fixed CHROMA_PATH, COLLECTION_NAME, MIN_DOC_COUNT"
      contains: "myth-vectors"
  key_links:
    - from: "rag_retriever.py CHROMA_PATH"
      to: "~/.jarvis/myth-vectors/"
      via: "Path.home() / '.jarvis' / 'myth-vectors'"
      pattern: "Path\\.home\\(\\).*myth-vectors"
---

<objective>
Fix the three broken constants in rag_retriever.py so the oracle RAG pipeline connects to the real ChromaDB that already exists and is populated.

Purpose: Oracle readings silently degrade to no-RAG mode because CHROMA_PATH points to a non-existent directory. The real DB is at ~/.jarvis/myth-vectors/ with collection myth_notes (218 Graves myth chunks). Three one-line fixes restore full RAG.

Output: Working rag_retriever.py that passes check_rag_ready() and returns myth chunks on query.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/claw2501/services/jarvis-sandbox/backend/oracle/rag_retriever.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CHROMA_PATH, COLLECTION_NAME, and MIN_DOC_COUNT</name>
  <files>/Users/claw2501/services/jarvis-sandbox/backend/oracle/rag_retriever.py</files>
  <read_first>
    Read lines 22-26 of /Users/claw2501/services/jarvis-sandbox/backend/oracle/rag_retriever.py
    to confirm current constant values before editing.
  </read_first>
  <action>
    Make exactly three surgical changes to the constants block (lines 22-26):

    1. Change CHROMA_PATH:
       FROM: `CHROMA_PATH = Path(__file__).parent.parent / "data" / "oracle_chroma"`
       TO:   `CHROMA_PATH = Path.home() / ".jarvis" / "myth-vectors"`

    2. Change COLLECTION_NAME:
       FROM: `COLLECTION_NAME = "oracle_wisdom"`
       TO:   `COLLECTION_NAME = "myth_notes"`

    3. Change MIN_DOC_COUNT:
       FROM: `MIN_DOC_COUNT = 1000`
       TO:   `MIN_DOC_COUNT = 200`

    Do NOT touch any other lines. The real ChromaDB is confirmed at
    ~/.jarvis/myth-vectors/ with UUID subdir bcd11e72-7440-4970-b50c-9427a6f99d42
    and collection myth_notes containing 218 Graves myth chunks.
    MIN_DOC_COUNT lowered to 200 so 218 docs passes the check_rag_ready() gate.
  </action>
  <verify>
    cd /Users/claw2501/services/jarvis-sandbox && python3 -c "
import sys; sys.path.insert(0, '.')
from backend.oracle.rag_retriever import check_rag_ready, retrieve_oracle_wisdom
print('check_rag_ready:', check_rag_ready())
results = retrieve_oracle_wisdom('Prometheus', ['Hero', 'Trickster'], ['fire', 'theft'])
print('retrieve results:', len(results), 'chunks')
print('PASS' if check_rag_ready() and len(results) > 0 else 'FAIL')
"
  </verify>
  <acceptance_criteria>
    - grep "Path.home()" /Users/claw2501/services/jarvis-sandbox/backend/oracle/rag_retriever.py returns a match
    - grep "myth-vectors" /Users/claw2501/services/jarvis-sandbox/backend/oracle/rag_retriever.py returns a match
    - grep "myth_notes" /Users/claw2501/services/jarvis-sandbox/backend/oracle/rag_retriever.py returns a match
    - grep "MIN_DOC_COUNT = 200" /Users/claw2501/services/jarvis-sandbox/backend/oracle/rag_retriever.py returns a match
    - Verify script above prints "PASS"
  </acceptance_criteria>
  <done>check_rag_ready() returns True against the real 218-chunk corpus. retrieve_oracle_wisdom() returns at least 1 chunk for a Greek myth query.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem→ChromaDB | Path change redirects DB access to ~/.jarvis/myth-vectors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | CHROMA_PATH | accept | Path is local filesystem under user home; no remote access |
| T-20-02 | Denial of Service | MIN_DOC_COUNT threshold | accept | Threshold is a soft gate; if collection drops below 200 RAG disables gracefully |
</threat_model>

<verification>
cd /Users/claw2501/services/jarvis-sandbox && python3 -c "
from backend.oracle.rag_retriever import CHROMA_PATH, COLLECTION_NAME, MIN_DOC_COUNT, check_rag_ready
assert 'myth-vectors' in str(CHROMA_PATH), f'Wrong path: {CHROMA_PATH}'
assert COLLECTION_NAME == 'myth_notes', f'Wrong collection: {COLLECTION_NAME}'
assert MIN_DOC_COUNT == 200, f'Wrong threshold: {MIN_DOC_COUNT}'
assert check_rag_ready(), 'check_rag_ready() returned False'
print('ALL CHECKS PASS')
"
</verification>

<success_criteria>
- CHROMA_PATH resolves to ~/.jarvis/myth-vectors (Path.home() / ".jarvis" / "myth-vectors")
- COLLECTION_NAME == "myth_notes"
- MIN_DOC_COUNT == 200
- check_rag_ready() returns True
- retrieve_oracle_wisdom() returns > 0 chunks for a myth query
</success_criteria>

<output>
After completion, create `/Users/claw2501/.planning/phases/20-pantheon-oracle-expansion/20-01-SUMMARY.md`
</output>
