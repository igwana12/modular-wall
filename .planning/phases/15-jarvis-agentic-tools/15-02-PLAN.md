---
phase: 15-jarvis-agentic-tools
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - /Users/claw2501/esp32-jarvis/bridge/test_agentic_tools.py
autonomous: false
requirements:
  - AGEN-02
  - AGEN-03
  - AGEN-05

must_haves:
  truths:
    - "Path traversal attempts are blocked and return an error string (not raise)"
    - "Non-allowlisted shell commands are refused and return an error string"
    - "Loop iteration cap logic is exercised and voices a failure summary"
    - "User verifies the full voice-to-code flow on R1 hardware"
  artifacts:
    - path: "/Users/claw2501/esp32-jarvis/bridge/test_agentic_tools.py"
      provides: "Automated unit tests for sandboxed tools and loop cap"
      exports: ["test_path_traversal_blocked", "test_sandbox_allows_valid_path", "test_allowlist_blocks_unapproved", "test_allowlist_passes_approved", "test_loop_cap"]
  key_links:
    - from: "test_path_traversal_blocked"
      to: "_assert_safe_path()"
      via: "direct call with ../../etc/passwd"
      pattern: "ERROR: Path traversal blocked"
    - from: "test_allowlist_blocks_unapproved"
      to: "_tool_exec_shell()"
      via: "asyncio.run() with 'rm -rf /'"
      pattern: "ERROR: Command not allowed"
---

<objective>
Write automated unit tests for the Phase 15 security boundaries, then perform a
manual end-to-end voice verification on R1 hardware.

The tests cover: path traversal blocking, sandbox allow for valid paths, exec_shell
allowlist enforcement, and loop cap behaviour. The checkpoint verifies the full
voice-to-code flow from the R1 device.

Purpose: Confirm security invariants hold before treating this phase complete.
R1 hardware is required for the checkpoint — plan is not autonomous.

Output: test_agentic_tools.py passing all tests + user-verified voice flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/claw2501/.planning/phases/15-jarvis-agentic-tools/15-RESEARCH.md
@/Users/claw2501/.planning/phases/15-jarvis-agentic-tools/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write unit tests for sandboxed tools</name>
  <files>/Users/claw2501/esp32-jarvis/bridge/test_agentic_tools.py</files>
  <read_first>
    - Read /Users/claw2501/esp32-jarvis/bridge/orb_bridge.py lines identifying SANDBOX_ROOT, ALLOWED_COMMANDS, _assert_safe_path, _tool_exec_shell, run_build_loop — confirm exact names and signatures before writing tests
    - The 15-01-SUMMARY.md will have exact line numbers and any deviations from the plan
  </read_first>
  <behavior>
    - Test 1 (path traversal blocked): `_assert_safe_path("../../etc/passwd")` raises ValueError with "Path traversal blocked" in message
    - Test 2 (sandbox allows valid path): `_assert_safe_path("js/thinking-mesh.js")` returns a Path object that is relative to SANDBOX_ROOT
    - Test 3 (allowlist blocks unapproved): `asyncio.run(_tool_exec_shell("rm -rf /"))` returns string starting with "ERROR: Command not allowed"
    - Test 4 (allowlist passes approved): `asyncio.run(_tool_exec_shell("git status"))` returns a string that does NOT start with "ERROR:" (git status succeeds or returns git output)
    - Test 5 (loop cap produces summary): Mock the Anthropic client to always return stop_reason="tool_use" with a read_file tool call. After 5 iterations the returned string contains "limit" (case-insensitive)
  </behavior>
  <action>
    Create `/Users/claw2501/esp32-jarvis/bridge/test_agentic_tools.py` with pytest tests.

    Import the functions directly from orb_bridge (sys.path manipulation if needed):
    ```python
    import sys, os, asyncio, pytest
    sys.path.insert(0, os.path.dirname(__file__))

    # Import only the functions under test — do NOT import the whole module at
    # top level (triggers bridge startup side effects like ADB serial detection).
    # Use importlib to import specific names after mocking heavy dependencies.
    ```

    For the loop cap test, mock `anthropic_client.messages.create` to always return
    a response with `stop_reason="tool_use"` and a single read_file tool_use block.
    Also mock `send_tts_morphing` to a no-op coroutine so TTS does not fire during tests.
    Use `unittest.mock.patch` or `pytest-mock`.

    The test file must be runnable with:
    ```
    cd /Users/claw2501/esp32-jarvis/bridge && python3 -m pytest test_agentic_tools.py -v
    ```

    All 5 tests must pass. If the loop cap test is too complex to mock cleanly
    (due to bridge startup side effects), mark it `@pytest.mark.integration` and
    ensure the other 4 pass in the automated run. Document the decision in the SUMMARY.
  </action>
  <verify>
    cd /Users/claw2501/esp32-jarvis/bridge && python3 -m pytest test_agentic_tools.py -v
  </verify>
  <acceptance_criteria>
    - All pytest tests pass (exit code 0) or integration-marked tests are documented as skipped
    - test_path_traversal_blocked: confirms ValueError is raised with "Path traversal blocked" in message
    - test_sandbox_allows_valid_path: confirms returned Path is relative to SANDBOX_ROOT
    - test_allowlist_blocks_unapproved: confirms "rm -rf /" returns string containing "ERROR: Command not allowed"
    - test_allowlist_passes_approved: confirms "git status" does NOT return "ERROR:" prefix
    - test_loop_cap: either passes with mock OR is marked @pytest.mark.integration with a comment explaining why
    - No import errors — the test file imports cleanly
  </acceptance_criteria>
  <done>
    test_agentic_tools.py exists, all non-integration tests pass, security boundary tests confirm path traversal and allowlist enforcement.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 15-01 implemented the full agentic build loop in orb_bridge.py:
    - _BUILD_PATTERN regex added to classify_intent() (Priority 2c, before jarvis fallback)
    - route == "build" branch added to process_query() dispatching to run_build_loop()
    - run_build_loop(): fires "Working on it" TTS ACK, caps at 5 iterations, voices failure on cap
    - _tool_read_file / _tool_write_file: Path.resolve().is_relative_to(SANDBOX_ROOT) sandbox
    - _tool_exec_shell: ALLOWED_COMMANDS frozenset {"git status", "git diff"}
    - _tool_reload_frontend: ADB am start re-navigation using get_r1_serial()
    - Plan 15-02 Task 1 wrote unit tests for security boundaries — all pass

    R1 hardware is required for this checkpoint.
  </what-built>
  <how-to-verify>
    **Prerequisites:** R1 device connected via USB ADB. Bridge running: `python3 /Users/claw2501/esp32-jarvis/bridge/orb_bridge.py` in a separate Terminal.

    **Test 1 — Voice ACK (AGEN-05 part 1):**
    1. On R1, press PTT and say: "make the orb pulse faster"
    2. Expected: JARVIS immediately speaks "Working on it" before any file reads begin
    3. Pass: You hear "Working on it" within ~1 second of speech end
    4. Fail: Silence for >5 seconds, or JARVIS responds with a conversational reply instead

    **Test 2 — Full build flow (AGEN-01):**
    1. Say: "make the orb pulse faster"
    2. Wait 15-40 seconds while JARVIS reads thinking-mesh.js and writes a change
    3. Expected: R1 browser reloads, JARVIS voices a confirmation like "Done, I increased the pulse speed"
    4. Check: `ls -la /Users/claw2501/esp32-jarvis/r1-frontend/js/thinking-mesh.js` — mtime should be recent
    5. Pass: File was modified AND browser reloaded AND JARVIS voiced completion
    6. Fail: JARVIS says "I can't do that" or no file change or no browser reload

    **Test 3 — Path traversal block (AGEN-02):**
    1. Open bridge logs in Terminal
    2. In a Python REPL: `import sys; sys.path.insert(0, '/Users/claw2501/esp32-jarvis/bridge'); from orb_bridge import _assert_safe_path; _assert_safe_path('../../etc/passwd')`
    3. Expected: ValueError raised with "Path traversal blocked" in message
    4. Pass: Exception raised, no file read attempted

    **Test 4 — Command rejection (AGEN-03):**
    1. On R1, say: "run rm minus r f slash" (or construct a test that routes through exec_shell)
    2. Alternatively test directly: `from orb_bridge import _tool_exec_shell; import asyncio; print(asyncio.run(_tool_exec_shell("rm -rf /")))`
    3. Expected: Returns string starting with "ERROR: Command not allowed"
    4. Pass: "rm -rf /" is never executed

    **Test 5 — JARVIS voices "I can only edit the R1 frontend" (AGEN-02 voice response):**
    This is the LLM-level response when sandbox blocks a path. To test:
    1. The tool returns "ERROR: Path traversal blocked" to Claude
    2. Claude should respond with something like "I can only edit the R1 frontend"
    3. This is verified by the system prompt wording — if Claude returns a sensible refusal, pass
    4. Note: exact phrasing may vary since it's LLM-generated; "R1 frontend" or "r1-frontend" in reply counts as pass

    **Confirm STATE.md note:** Phase 15 hardware note in STATE.md says "R1 hardware required for validation" — mark as validated after this checkpoint passes.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 5 tests pass.
    Type "partial: [describe failures]" if some tests fail — execution will continue to create gap-closure plans.
    Type "blocked: no R1" if hardware is unavailable — phase will be marked pending hardware.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test imports → orb_bridge | Test file imports specific functions; bridge startup side-effects must not fire |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Tampering | test_agentic_tools.py import | accept | Tests import specific symbols only; if bridge startup side-effects cause test failures, document and mark affected tests as @pytest.mark.integration |
</threat_model>

<verification>
- `cd /Users/claw2501/esp32-jarvis/bridge && python3 -m pytest test_agentic_tools.py -v` exits 0
- Security boundary tests confirm: path traversal raises ValueError, allowlist blocks unapproved commands
- Manual checkpoint approved by user on R1 hardware
</verification>

<success_criteria>
- test_agentic_tools.py passes all automated tests (or integration tests documented as skipped)
- Path traversal test confirms ValueError with "Path traversal blocked" message
- Allowlist test confirms "rm -rf /" returns "ERROR: Command not allowed" and is never executed
- User has verified on R1: voice ACK fires immediately, full build flow modifies a file and reloads browser, voice confirms completion
</success_criteria>

<output>
After completion, create `.planning/phases/15-jarvis-agentic-tools/15-02-SUMMARY.md` with:
- Test results (pass/fail counts)
- Any integration-marked tests and why
- Hardware checkpoint result (approved / partial / blocked)
- Phase 15 overall completion status
</output>
