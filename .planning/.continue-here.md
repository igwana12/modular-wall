---
phase: ad-hoc/3d-logo-pipeline
task: complete
status: paused — ready to batch
last_updated: 2026-03-31T02:32:39.276Z
---

<current_state>
3D logo turntable pipeline is WORKING. Three hero logos (gorilla, zeus, rosette) have been
rendered as 240-frame MP4s with correct motion: logo stands upright facing camera, spins
around its vertical axis like a globe (face → edge → back → edge → face).

The Blender script is at two locations:
- SOURCE OF TRUTH (edit here): /Volumes/AI_WORKSPACE/jarvis-visuals/3d-logos/blender_extrude_render.py
- REPO COPY (for versioning): /Users/claw2501/tools/3d-logo-pipeline/blender_extrude_render.py

Rendered outputs: /Volumes/AI_WORKSPACE/jarvis-visuals/3d-logos/renders/
  - gorilla-profile-turntable.mp4
  - zeus-lightning-turntable.mp4
  - rosette-turntable.mp4
</current_state>

<completed_work>

- Vectorized 3 logos to SVG (Zeus, Gorilla, Rosette) via potrace
- Blender extrude + render pipeline built from scratch
- Fixed critical animation bug: 0°/360° keyframe wrapping produced static frames — replaced with baked per-frame keyframes (one keyframe per frame, explicit trig)
- orient_upright(): rotates logo +90° around X so face points toward -Y (camera)
- setup_globe_spin(): Z-axis rotation, 240 frames baked, linear interpolation
- setup_fixed_camera(): camera at (0, -d, -d*0.25) — slightly below, looking up
- Metallic silver material (0.85, 0.85, 0.88), metallic=0.95, roughness=0.2
- 240 frames @ 24fps = 10s per loop (slow, intentional)
- Logo depth = 1/10 of face height (thin but visible edge)
- Coin disc approach built and tested but discarded — user wanted logo only
</completed_work>

<remaining_work>

## Immediate next step
Batch-process the remaining ~66 logo images from ~/Downloads through the full pipeline:
  1. vectorize_v2.py — clean + trace each image to SVG
  2. blender_extrude_render.py — extrude + render each SVG to MP4

## After batch
Wire 3D spin animations into JARVIS MediaDirector as contextual overlays:
  - Map deity/entity → logo symbol
  - Trigger: when JARVIS mentions a deity, play that deity's spinning logo as overlay
  - Integration point: JARVIS media overlay system (vignette frame)
  - Overlay should loop during the deity passage, fade out after

## Future refinements
- Adjust spin speed per context (fast = excited, slow = reverent)
- Add glow/bloom post-processing for more dramatic look
- Consider transparent background (alpha) renders for web overlay use
</remaining_work>

<decisions_made>

- Globe spin (Z-axis object rotation, fixed camera) > camera orbit — cleaner, user confirmed
- Logo only, no coin disc — user confirmed "I just cared about the motion"
- Silver metallic > gold — user preference
- 240 frames @ 24fps (10s loop) — slow and deliberate, user approved
- Baked per-frame keyframes — required because Blender 5.x treats 0°/360° as identical
- orient_upright uses +90° around X (not -90°) — face normal goes to -Y where camera is
- Depth ratio 1/10 of face height — thin but catches light
- target_size=2.0 in center_and_scale — logo fills frame nicely
</decisions_made>

<blockers>
None. Pipeline is clean and repeatable.
</blockers>

<context>
The pipeline was built iteratively this session with a lot of back-and-forth on the motion.
The key breakthrough was understanding the user wanted the classic video game coin spin —
logo stands up like a medal, spins in place around its vertical axis. NOT rolling, NOT tumbling,
NOT camera orbit. Object rotates, camera is fixed and slightly below looking up.

The baked keyframe approach (one keyframe per frame) is the critical architectural decision
because Blender 5.x's animation system treats 360° and 0° as identical and produces no motion
with the simple two-keyframe approach.

There are 69 total logo images in ~/Downloads (timestamps from ~7:58 PM). Three processed.
66 remain. The vectorize_v2.py + blender_extrude_render.py scripts are reusable as-is.
</context>

<next_action>
Start with: batch the remaining 66 logos.

Run this to identify all unprocessed images:
  ls ~/Downloads/IMG_7*.jpg | sort  (or .HEIC — check extension)

Then for each image:
  1. python3 /Volumes/AI_WORKSPACE/jarvis-visuals/3d-logos/vectorize_v2.py <image> <name>
  2. blender --background --python /Volumes/AI_WORKSPACE/jarvis-visuals/3d-logos/blender_extrude_render.py -- <svg> <name> <render_dir>

Or write a batch script that loops through all unprocessed images.
After batch: /gsd:plan-phase for JARVIS MediaDirector integration.
</next_action>
