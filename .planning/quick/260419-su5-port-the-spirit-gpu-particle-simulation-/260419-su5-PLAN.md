---
phase: 260419-su5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/jarvis-v3-ui/src/spirit/spirit.js
  - services/jarvis-v3-ui/src/spirit/simulator.js
  - services/jarvis-v3-ui/src/spirit/particles.js
  - services/jarvis-v3-ui/src/spirit/glsl/
  - services/jarvis-v3-ui/src/spirit/lib/
  - services/jarvis-v3-ui/src/canvas.js
  - services/jarvis-v3-ui/src/audio-analyser.js
  - services/jarvis-v3-ui/src/visual-state.js
  - services/jarvis-v3-ui/src/main.js
  - services/jarvis-v3-ui/index.html
  - services/jarvis-v3-ui/vite.config.js
  - services/jarvis-v3-ui/package.json
autonomous: false
requirements:
  - SU5-01: Port The Spirit particle simulation to jarvis-v3-ui as ES modules
  - SU5-02: Wire mic input into physics params (bass→attraction, mid→speed, treble→curlSize)
  - SU5-03: Expose visual-state.js hooks so WS events can drive physics
  - SU5-04: Replace canvas placeholder, preserve ws-client/sse-client/mode switcher/transcript rail/status bar
user_setup: []

must_haves:
  truths:
    - "Opening http://127.0.0.1:8360/ shows The Spirit particle field rendering (not the old canvas placeholder)"
    - "Granting mic permission and making sound visibly changes the particle motion (speed/attraction/curl respond to audio)"
    - "Denying mic permission still renders particles (time-based fallback, original Spirit behavior)"
    - "Mode switcher, transcript rail, status bar, ws-client, sse-client still function unchanged"
    - "visual-state.js exposes a setPhysicsParams(speed, attraction, curlSize) hook that WS events can call"
  artifacts:
    - path: "services/jarvis-v3-ui/src/spirit/spirit.js"
      provides: "ES module entry — init(canvas), update(dt, audioBands), dispose()"
      exports: ["init", "update", "dispose", "setPhysicsParams"]
    - path: "services/jarvis-v3-ui/src/spirit/simulator.js"
      provides: "GPU particle physics (ported from The-Spirit, ES module)"
    - path: "services/jarvis-v3-ui/src/spirit/particles.js"
      provides: "Particle rendering mesh + material (ES module)"
    - path: "services/jarvis-v3-ui/src/spirit/glsl/"
      provides: "All 14 shader files from The-Spirit repo, loaded via Vite ?raw imports"
    - path: "services/jarvis-v3-ui/src/canvas.js"
      provides: "Thin wrapper that mounts spirit.js into the existing canvas element and runs the animation loop"
    - path: "services/jarvis-v3-ui/src/audio-analyser.js"
      provides: "Mic AnalyserNode + getBands() returning {bass, mid, treble} each frame, 0..1"
      exports: ["initAudio", "getBands", "isActive"]
    - path: "services/jarvis-v3-ui/src/visual-state.js"
      provides: "setPhysicsParams hook wired into spirit.js; blob_morph/voice_sigil WS events route here"
  key_links:
    - from: "src/main.js"
      to: "src/canvas.js"
      via: "import + init on DOMContentLoaded"
      pattern: "import.*canvas"
    - from: "src/canvas.js"
      to: "src/spirit/spirit.js"
      via: "import init/update, call each frame"
      pattern: "from.*spirit/spirit"
    - from: "src/canvas.js"
      to: "src/audio-analyser.js"
      via: "getBands() called each frame, passed to spirit.update()"
      pattern: "getBands"
    - from: "src/visual-state.js"
      to: "src/spirit/spirit.js"
      via: "setPhysicsParams re-exported through visual-state"
      pattern: "setPhysicsParams"
    - from: "src/ws-client.js"
      to: "src/visual-state.js"
      via: "blob_morph / voice_sigil events call setPhysicsParams"
      pattern: "blob_morph|voice_sigil"
---

<objective>
Port edankwan/The-Spirit GPU particle simulation into services/jarvis-v3-ui as ES modules, replacing the existing canvas placeholder. Wire Web Audio mic input so bass/mid/treble drive physics (attraction/speed/curlSize). Expose hooks so WS events (blob_morph, voice_sigil) can override physics. Preserve the existing shell: ws-client, sse-client, mode switcher, transcript rail, status bar.

Purpose: give JARVIS V3 a real signature visual. The Spirit is mouse-driven curl-noise particles — swapping mouse for mic + WS makes it audio-reactive and event-driven without rewriting the physics engine.

Output: a working /src/spirit/ directory, rewired canvas.js/audio-analyser.js/visual-state.js, updated index.html, and a dev server on 8360 that renders particles reacting to mic audio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<notes>
Target directory: /Users/claw2501/services/jarvis-v3-ui/
Current files (verified via ls + wc):
- src/canvas.js (262 lines) — current visual placeholder, to be rewired as Spirit host
- src/audio-analyser.js (105 lines) — already exists, needs getBands() contract confirmed/added
- src/visual-state.js (65 lines) — needs setPhysicsParams hook
- src/main.js (207 lines) — wires everything; leave WS/SSE/mode/transcript untouched
- package.json: three@^0.160.0 already a dep; type: module; vite@^5
- Port: 8360 (vite --host 127.0.0.1)

The Spirit source: https://github.com/edankwan/The-Spirit (MIT). Key files:
- src/index.js → adapt as src/spirit/spirit.js (ES module entry)
- src/3d/simulator.js → src/spirit/simulator.js (GPU physics via THREE.WebGLRenderTarget ping-pong)
- src/3d/particles.js → src/spirit/particles.js (InstancedMesh rendering)
- src/glsl/*.vert, *.frag (14 files) → src/spirit/glsl/ (loaded via `?raw` Vite imports)
- Any small helpers (fboHelper, mouse/motionBlur) → src/spirit/lib/ as needed or stubbed out

Browserify→ESM rewrite rules:
- `var x = require('./foo')` → `import x from './foo.js'`
- `module.exports = X` → `export default X`
- `module.exports.foo = ...` → `export const foo = ...`
- GLSL was likely loaded via `brfs` or string inlining → use Vite `import frag from './glsl/render.frag?raw'`
- Three.js globals (`THREE.XXX`) → `import * as THREE from 'three'`
- Drop the mouse-driven input pipe entirely; replace with audioBands param on update()

Physics param targets (from planning_context):
- speed: 0.5 → 3.0 (mid freq)
- attraction: 0.02 → 0.15 (bass)
- curlSize: 0.2 → 1.5 (treble)
Lerp at ~0.1 per frame so audio swings don't snap.

Audio fallback: if getUserMedia denied/errors, getBands() returns time-based sinewaves so the simulation still breathes (original Spirit time-driven behavior).

DO NOT TOUCH these files beyond the minimum hook additions:
- src/ws-client.js (only wire blob_morph/voice_sigil → visual-state if not already)
- src/sse-client.js (leave alone)
- src/cinematic-hud.js, operator-panel.js, diagnostics-panel.js, media-panel.js (leave alone)
- src/style.css (leave alone unless canvas sizing breaks)
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port The Spirit source into src/spirit/ as ES modules</name>
  <files>services/jarvis-v3-ui/src/spirit/spirit.js, services/jarvis-v3-ui/src/spirit/simulator.js, services/jarvis-v3-ui/src/spirit/particles.js, services/jarvis-v3-ui/src/spirit/lib/, services/jarvis-v3-ui/src/spirit/glsl/ (14 shader files)</files>
  <action>
    Clone The-Spirit into a temp location (do NOT add as submodule):
    `git clone --depth 1 https://github.com/edankwan/The-Spirit /tmp/the-spirit-src`

    Create services/jarvis-v3-ui/src/spirit/ and copy + rewrite:

    1. Copy all 14 .frag/.vert files from /tmp/the-spirit-src/src/glsl/ → src/spirit/glsl/ unchanged.

    2. Port /tmp/the-spirit-src/src/3d/simulator.js → src/spirit/simulator.js:
       - `require('three')` → `import * as THREE from 'three'`
       - Each GLSL `require('../glsl/foo.frag')` → `import fooFrag from './glsl/foo.frag?raw'`
       - `module.exports = { init, update }` → `export function init() {}; export function update() {}` or class pattern preserving original structure
       - Preserve ping-pong WebGLRenderTarget pattern for position/velocity textures
       - Add exported setters: `setSpeed(v)`, `setAttraction(v)`, `setCurlSize(v)` (thin wrappers over the existing uniform values)

    3. Port /tmp/the-spirit-src/src/3d/particles.js → src/spirit/particles.js same way. Preserve the render material + geometry; rewrite imports/exports only. Drop any mouse-tracker references.

    4. Port /tmp/the-spirit-src/src/index.js → src/spirit/spirit.js as the public entry. Strip: DOM bootstrap, stats.js, dat.GUI, mouse handlers, motionBlur/fxaa post (or keep post if trivial — skip if it drags in >2 extra files). Export:
       ```js
       export function init(canvas) { /* build renderer+scene+simulator+particles */ }
       export function update(dt, bands) {
         // bands = { bass, mid, treble } 0..1
         const speed      = lerp(current.speed,      0.5  + bands.mid    * 2.5,  0.1);
         const attraction = lerp(current.attraction, 0.02 + bands.bass   * 0.13, 0.1);
         const curlSize   = lerp(current.curlSize,   0.2  + bands.treble * 1.3,  0.1);
         simulator.setSpeed(speed); simulator.setAttraction(attraction); simulator.setCurlSize(curlSize);
         simulator.update(dt); particles.update(dt); renderer.render(scene, camera);
       }
       export function setPhysicsParams({ speed, attraction, curlSize }) { /* hard override for WS events */ }
       export function dispose() { /* cleanup */ }
       ```

    5. If simulator/particles reference a fboHelper or similar internal lib, drop it in src/spirit/lib/ with the same ESM rewrite.

    6. Update vite.config.js if needed to ensure `?raw` glsl imports and `/src/spirit/` served correctly (Vite supports `?raw` natively, likely no config change needed — verify).

    Keep the port minimal: no new features, no style changes, no post-processing unless it comes free. If a file has >50 lines of mouse/GUI/stats code, delete those sections and leave a `// removed: mouse/GUI/stats per SU5-01` comment.

    At end of task: clean up `/tmp/the-spirit-src`.

    Attribution: add `// Ported from edankwan/The-Spirit (MIT) — https://github.com/edankwan/The-Spirit` header to spirit.js, simulator.js, particles.js.
  </action>
  <verify>
    <automated>cd /Users/claw2501/services/jarvis-v3-ui && npx vite build 2>&1 | tee /tmp/su5-build.log; grep -E "(error|Error|ERR)" /tmp/su5-build.log && exit 1 || echo OK</automated>
    Manual: `ls src/spirit/glsl/ | wc -l` returns ≥ 14.
    Manual: `grep -r "require(" src/spirit/` returns zero hits.
    Manual: `grep -r "module.exports" src/spirit/` returns zero hits.
  </verify>
  <done>
    src/spirit/ exists with spirit.js, simulator.js, particles.js, glsl/ (≥14 files), optional lib/. No require() or module.exports anywhere in src/spirit/. `npx vite build` completes without shader/import errors. spirit.js exports init, update, setPhysicsParams, dispose.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire mic audio → spirit physics and expose WS hook via visual-state</name>
  <files>services/jarvis-v3-ui/src/canvas.js, services/jarvis-v3-ui/src/audio-analyser.js, services/jarvis-v3-ui/src/visual-state.js, services/jarvis-v3-ui/index.html</files>
  <action>
    1. **src/audio-analyser.js** — confirm/add the contract spirit.js expects:
       ```js
       export async function initAudio() { /* getUserMedia({audio:true}) → AudioContext → AnalyserNode(fftSize:256) */ }
       export function getBands() {
         // if !active: return time-based defaults so Spirit still breathes
         //   { bass: 0.3 + 0.2*Math.sin(t*0.7), mid: 0.3 + 0.2*Math.sin(t*1.1), treble: 0.3 + 0.2*Math.sin(t*1.7) }
         // else: analyser.getByteFrequencyData(buf), average buckets:
         //   bass 20-250Hz (bins 0-5), mid 250-4000Hz (bins 6-40), treble 4000-20000Hz (bins 41-127)
         //   normalize /255 → clamp 0..1
       }
       export function isActive() { /* true iff mic stream connected */ }
       ```
       If current audio-analyser.js already has some of this, extend it rather than rewrite. Keep mic opt-in: don't auto-request on module load — expose `initAudio()` and have canvas.js call it on first user gesture (mousedown/keydown) to satisfy browser autoplay policies.

    2. **src/canvas.js** — replace the existing placeholder animation with Spirit host:
       - Grab the existing `<canvas>` element (index.html currently has a canvas placeholder — keep the same id/class so CSS sizing works)
       - `import { init, update } from './spirit/spirit.js'`
       - `import { initAudio, getBands } from './audio-analyser.js'`
       - On DOMContentLoaded: `init(canvas)`; attach one-time listener for first user gesture → `initAudio().catch(() => {/* fallback to time-based bands */})`
       - Animation loop: `requestAnimationFrame` → compute dt → `update(dt, getBands())`
       - Handle resize: on window resize, update renderer + camera aspect (spirit.js should expose a resize helper or canvas.js can call `renderer.setSize` directly via a re-exported function)
       - Preserve the export surface canvas.js currently provides to main.js (if main.js imports anything from canvas.js, keep those names; thin-wrap as needed)

    3. **src/visual-state.js** — add the WS hook:
       ```js
       import { setPhysicsParams } from './spirit/spirit.js';
       export function applyVisualEvent(event) {
         // event.type === 'blob_morph' → { speed, attraction, curlSize } directly
         // event.type === 'voice_sigil' → preset mapping per sigil id
         if (event.type === 'blob_morph' || event.type === 'voice_sigil') {
           setPhysicsParams(mapEventToParams(event));
         }
       }
       ```
       Keep existing visual-state.js exports intact. If ws-client.js already routes events through visual-state, just add the new handler path; do not modify ws-client.js logic beyond the call site if it's not already wired.

    4. **index.html** — if the current canvas element has placeholder-specific attributes/IDs that Spirit's init expects to be different, reconcile. Preferred: keep the existing `<canvas id="jarvis-canvas">` (or whatever it's named) and pass it into `init(canvas)`. Do NOT remove the transcript rail, status bar, mode switcher DOM.

    Do not touch: ws-client.js (except adding one dispatch line to applyVisualEvent if missing), sse-client.js, cinematic-hud.js, operator-panel.js, diagnostics-panel.js, media-panel.js, style.css.
  </action>
  <verify>
    <automated>cd /Users/claw2501/services/jarvis-v3-ui && npx vite build 2>&1 | tail -30 && node -e "const fs=require('fs'); const v=fs.readFileSync('src/visual-state.js','utf8'); const c=fs.readFileSync('src/canvas.js','utf8'); const a=fs.readFileSync('src/audio-analyser.js','utf8'); if(!v.includes('setPhysicsParams')) throw new Error('visual-state missing setPhysicsParams'); if(!c.includes('spirit/spirit')) throw new Error('canvas not importing spirit'); if(!a.includes('getBands')) throw new Error('audio-analyser missing getBands export'); console.log('wiring OK');"</automated>
  </verify>
  <done>
    Build succeeds. canvas.js imports spirit.js and calls update() in RAF. audio-analyser.js exports initAudio/getBands/isActive with mic-opt-in + time-based fallback. visual-state.js exports applyVisualEvent that routes blob_morph/voice_sigil to setPhysicsParams. index.html canvas element untouched in ID/class. ws-client.js, sse-client.js, mode switcher, transcript rail, status bar left alone.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies particle sim + audio reactivity in browser</name>
  <what-built>
    The Spirit particle field now renders in place of the canvas placeholder. Mic input (when granted) drives bass→attraction, mid→speed, treble→curlSize. Time-based fallback when mic denied. visual-state.applyVisualEvent() routes WS blob_morph/voice_sigil events to spirit.setPhysicsParams(). All existing shell (ws-client, sse-client, mode switcher, transcript rail, status bar) intact.
  </what-built>
  <how-to-verify>
    In a Terminal window (NOT via the Bash tool — this is a long-running dev server):
    1. `cd ~/services/jarvis-v3-ui && npm run dev`
    2. Open http://127.0.0.1:8360/
    3. Expect: particle field visible (curl-noise flow, not a static canvas).
    4. Click anywhere to trigger first-gesture → mic permission prompt appears. Grant it.
    5. Talk / clap / play music near the mic. Expect: particle motion visibly reacts — bass thumps pull particles inward (attraction ↑), mids speed the flow, trebles break up the curl pattern.
    6. Deny mic (refresh and click Deny). Expect: particles still animate (time-based fallback), no console errors.
    7. Switch between Cinematic / Operator / Diagnostics modes. Expect: mode switcher still works, particle canvas stays put or swaps per mode as before.
    8. Check the transcript rail + status bar are still rendered and functional.
    9. Open devtools Console. Expect: no red errors. WebGL warnings about extensions are OK.
    10. If WS is connected, trigger (or simulate) a `blob_morph` event with `{speed:2.5, attraction:0.14, curlSize:1.4}`. Expect: visible physics jump to those values.

    Fail conditions:
    - Blank canvas / WebGL context error → simulator port bug, likely shader import path
    - Particles render but mic grant does nothing → audio-analyser bucket math or canvas.js not passing bands into update()
    - Mode switcher broken / transcript rail gone → scope creep in Task 2, roll back index.html edits
    - Console spam of shader compile errors → `?raw` import missing for one of the 14 glsl files
  </how-to-verify>
  <resume-signal>Type "approved" or paste the specific symptom (blank canvas / mic not reacting / shell broken / console errors).</resume-signal>
</task>

</tasks>

<verification>
- Dev server at 127.0.0.1:8360 renders The Spirit particle field (not placeholder)
- Mic audio drives physics (audible → visible reaction)
- Mic denial → time-based fallback still animates
- visual-state.applyVisualEvent({type:'blob_morph',...}) overrides physics
- ws-client, sse-client, mode switcher, transcript rail, status bar unchanged
- No console errors; `npx vite build` succeeds
- src/spirit/ is pure ESM (no require/module.exports)
- MIT attribution header in ported files
</verification>

<success_criteria>
Human approval on Task 3 checkpoint. All verification items pass. No regressions in the existing shell. Build succeeds.
</success_criteria>

<output>
After completion, create `.planning/quick/260419-su5-port-the-spirit-gpu-particle-simulation-/260419-su5-SUMMARY.md` with:
- Ported files + shader count
- Audio mapping ranges actually used
- Any Spirit features dropped during port (mouse, GUI, stats, post-fx) and why
- Screenshot/recording path if captured during checkpoint
- Gotchas encountered (shader path issues, Three.js version mismatches, audio context autoplay quirks)
</output>
